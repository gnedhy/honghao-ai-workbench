# 工作提交按钮与品牌区精简验证记录

日期：2026-08-17

## 调整内容

1. 工作输入区移除“交给智能体执行”可见文字，统一为 36px 圆形向上箭头按钮。
2. 按钮在空输入时保持禁用，输入工作要求后可提交；无障碍名称仍为“交给智能体执行”。
3. “宏昊 AI”品牌区移除下拉箭头、按钮和“切换工作空间”语义，避免产生当前并不存在的工作空间切换预期。
4. 聊天输入区、已有对话、知识库、任务流程及安全审批边界不变。

## 验证结果

- `npm.cmd run typecheck`：通过。
- `npm.cmd run build`：通过。
- 桌面端浏览器验证：提交按钮为 36px 圆形，无可见文字；品牌区无下拉箭头；页面无横向溢出。
- 截图：[工作提交按钮与品牌精简（桌面端）](./11-工作提交按钮与品牌精简-桌面端.png)
