# 界面设计约定

## 2026-09-12：统一分段选择样式

本轮用户已批准研发工作台注释调整，并确认同类分段按钮复用反馈窗口的白色选中底。

- 全局 `sliding-segments` 与 `segmented-control` 使用浅灰轨道、白色选中底和轻阴影，保留滑移动效及减少动态支持。
- CSS 模块中的选中按钮保持透明，由共用指示底统一绘制；不改变按钮文案、行为与权限。
- 同类菜单、日期范围与表格继续以现有采购控件为设计权威。页面布局与业务状态另由对应 surface brief 维护。
- 其它颜色、字体、布局及已有界面约定沿用现有实现。

机器可读约定同步保存在 `.impeccable/design.json`，与本文件共同记录本次全局组件变更。
