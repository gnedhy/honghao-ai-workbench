# 部门组织与人员归属

## 已确认

- 用户确认 A 紧凑树形。初始总经办1人、采购部5人，主部门来自现有账号资料；不迁入预览的子部门、兼属关系或测试账号。
- 管理员维护多级部门与一主多兼属关系。部门仅组织人员，不计算业务权限，不复用采购部门/原料分配表。
- “我的账号”中的姓名、部门和权限均为只读。管理员统一在用户管理维护资料，也可修改自己的姓名和部门，但不能修改自己的授权或停用自身。

## 实现

- identity schema 5 → 6；新增 organization_departments、organization_memberships，迁移标记 organization_schema_version=1。首轮把既有文本部门迁入主部门，重跑不会重复创建。
- `GET/POST /api/departments`、`PATCH/DELETE /api/departments/{id}`：管理员部门维护；同级重名、防循环、非空删除服务端检查。
- `PUT /api/users/{id}/departments` 更新归属；管理员资料 PATCH 可携带 membership，把姓名与归属原子保存。主部门唯一、兼属不能重复或包含主部门。
- 旧 department 展示字段保留，随主部门名称同步；账号ID、密码、访问权限和历史姓名快照不变。旧内部/管理员创建账号的文本部门兼容为根部门归属；正常界面使用部门ID。
- 复用设置中的放弃修改弹窗作为删除确认；写入成功与刷新失败区分提示，避免误导重复提交。部门操作、人员归属调整记录审计。

## 当次发布检查（历史证据）

- 全量后端 pytest 通过；组织/个人资料/身份/权限的40项目标回归通过；前端26项回归通过。
- 类型检查、候选构建及安全门禁通过；本轮界面机械扫描无发现。
- 真实独立候选8043：新增子部门、主/兼属调整、重复人员显示但账号总数去重、保存/取消/草稿保护、非空删除、窄屏、键盘、普通用户接口拒绝均通过。候选仅示例数据，正式密码未用于测试。
- 对正式数据库的独立副本执行两次迁移：所有旧表逐行相等，完整性与外键检查通过；结果总经办1、采购部5。

## 发布

- 已发布到正式8000服务，production / ready；工作台active、采购active，其他工作台模式未变。
- 正式快照：`C:/Users/gnedhy/AppData/Local/HonghaoAI/production/backups/organization-release-20260910/snapshot-20260910T134015019797Z`；同级`previous-dist`保留此前前端。Windows桌面包实际落在LocalCache对应数据目录。
- 停服后对正式数据库执行两次幂等初始化并逐行核对所有旧表，零差异；总经办1、采购部5。未写入任何候选测试关系。
- 当次发布前端入口（历史）：index-bBn2jKlM.js / index-CmOYuPX3.css。发布时正式浏览器已验证组织树、当前账号及采购概览仍正常，未操作实际人员归属或授权。后续构建与收尾核对见 [提交准备](./提交准备-2026-09-10.md)。
- 回退须先停服并保全发布后新增数据，使用快照进行有选择的恢复；不要用旧整库覆盖发布后业务。
