# 用户反馈正式部署

## 已批准范围

用户批准搜索右侧常驻反馈入口、未读角标、普通用户本人列表、管理员全部列表及“仅看我提交的”、固定顶部返回和固定底部操作。最后明确要求复用系统设置的未保存确认弹窗，并更新正式服。

实现采用真实账号会话、SQLite反馈与逐账号已读记录；截图存入数据库以随快照备份。反馈处理状态和已读状态独立，管理员完成时必须填写处理结果；内容版本用于防止并发覆盖及旧详情误清新通知。普通用户只能访问本人内容和截图，管理员可处理全部反馈。未读入口每30秒及页面恢复焦点时刷新，失败不伪装为零。

正式发布前备份已建立在原正式数据目录 backups/feedback-release-20260910，包含 snapshot-20260910T115628493747Z 数据和配置快照、previous-dist 旧页面。实际发布和检查结果在下节追加；当前不代表已发布。

## 当前发布状态

2026-09-10 20:15（北京时间）正式发布完成。此前拒绝为历史记录，本次正常发布命令成功执行，未修改 Codex 安全配置。发布前新建完整快照 `backups/feedback-release-20260910/snapshot-20260910T121539296990Z`，复制已验收候选构建至正式 `dist` 并重启服务；沿用原 production 数据目录，未迁入候选账号或测试反馈。

发布后 `/api/health` 返回 `production/ok`，`/api/readiness` 全项通过；HTTP 首页引用 `index-BjW7XgOs.js`，正式与候选首页 SHA-256 一致（`E9B19F6437BD17305465E43578C89A878765AB98E90BBC4185B6A49801476033`）。在原有 gnedhy 登录会话刷新正式页面，搜索旁反馈入口已出现；打开“用户反馈”正常显示 0 条记录、管理员“仅看我提交的”筛选与新建反馈入口，无加载错误。关于系统的反馈与结果通知更新日志包含在本次构建内。本次只做正式环境只读冒烟检查，提交、处理及已读闭环沿用上文候选验证，不向正式库写入演示反馈。

### 此前受阻记录

候选8036真实闭环验证通过，反馈和个人资料相关18项测试、安全门禁、独立安全与视觉审查通过。已复用设置的放弃修改确认弹窗。2026-09-10 尝试发布时，组合发布命令被宿主执行策略拒绝，命令未执行；未尝试替代渠道绕过。随后只读确认8000仍为production且ready，HTTP首页与候选构建不同，因此本次尚未发布。后续需解除执行限制再继续，不得把候选完成认作正式发布完成。

用户随后要求继续并完善更新日志。“关于系统”已新增意见反馈与结果通知、关于系统两项更新说明，候选重新构建及安全检查通过。正常发布流程再次被同一执行策略拒绝，尚未执行；正式8000仍ready且首页与新候选不同。当前待发布构建为 index-BjW7XgOs.js；先前 deployed-source-and-dist.zip 不包含本次新增日志，不可作为最新发布构建使用。

## 回退原则

分段选择后续更新：正式构建 `index-CjcBGzdi.js`。反馈类型仅在键盘 focus-visible 时显示焦点环；鼠标切换采用共享 `sliding-segments` 白色指示块（200ms ease-out）。复用于采购涨跌排行、台账浏览方式、审计日志浏览方式及日志页数档位；已有 SegmentedControl 对齐尺寸和缓动。支持两/三等宽选项，减少动态效果时关闭过渡。类型检查与构建通过，正式服务 ready，浏览器鼠标选中功能建议实测无蓝框。未改字号滑杆、权限开关或业务状态。

后续 UI 更新：用户批准 A 单列布局、本人筛选按钮和采购风格下拉，并批准沿用设置动效。已发布前端 `index-BFxMPCI2.js` / `index-X_p6hxog.css`，构建通过；正式浏览器验证打开 → 新建 → 关闭 → 焦点返回入口。未改后端和业务数据。动效使用已有关键帧及缓动，关闭新增可清理的 150ms 延迟，减少动态效果模式跳过延迟。

保留新建反馈表及新数据，回退代码和页面不恢复旧数据库；只有数据损坏才使用快照，避免覆盖发布后新业务。候选账号与测试反馈不迁入正式库。本次不包含提交、推送或清场。
