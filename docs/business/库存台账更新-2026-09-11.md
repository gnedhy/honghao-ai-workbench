# 库存台账更新记录

## 后续显示调整

用户按截图要求库存量不显示小数、悬停查看完整数值，增加数量升降序切换和同“价格变化”的说明图标，列名改为“库存价格”。用户随后明确不使用四舍五入，最终仅显示整数部分，例如 21016.687 显示 21016；完整值保留在原生悬停提示中，排序仍按原始小数，未修改库存数据。库存量显示相同的记录也按原始数值排序，缺失值始终置后。

进一步确认：实际零库存和缺失库存量显示 —，参考库存价格的显示方式；原始零值仍参与数量排序。有库存但不足 1 kg 的原料仍仅显示整数部分，悬停显示其完整正数，不将其当成无库存。

27 项前端测试及类型检查、候选构建通过；桌面和窄屏界面、双向排序、提示属性、复用图标、零库存及页面错误检查通过。后续截去小数、零库存显示 — 的修正再次通过类型检查和构建，正式页面已核对 CF298 显示 24666、CF186 显示 21016、CF392D 显示 —，悬停保留原数值。此增量只替换前端资源，无需重启 API。当前证据为 `20260911-inventory-truncate/manifest.json`；以下为初始数据更新记录。

用户已确认并授权实施：只更新现有 138 个在用原料，在库存量列保留源精度和零值；库存价为空或零均视为缺失；正式价格变化继续与前一正式版本比较。

## 来源与结果

- 原件：`C:\Users\gnedhy\Desktop\原料成本\0908 原料行情表.xlsx`，只读取“原料行情总表”，B 列原料编号、C 列库存数量、I 列库存价格。原件未修改。
- SHA-256：`4022d94a5bc51ef7965017fd0aef862eb6bb782c756679c5de6aff23967f4cf7`。
- 138 个编号全部匹配，无重复、缺失或新增；127 项正库存、11 项零库存；126 项有效库存价、12 项缺失。
- CF012 的原始库存价为 0，独立库存记录保留原文 `0`，对外库存价为 null、页面显示 —。11 个原表空白同样显示 —，其中 CF094、CF150B、CF004 有库存但未提供库存价，不作推算。
- 库存使用原料现有 kg 单位；报表文件名仅作为来源版本，不冒充已核实的实时库存日期。数量保留全部有效小数位。
- 已更新正式数据并于 2026-09-11 16:19 重启；仍为 138 个原料、20 个正式价格版本。更新前后及重启后，20 张既有采购业务表的逐表内容指纹完全相同。

## 实现与验证

采购结构升级到 v6，独立 `procurement_inventory` 记录关联原料 ID；概况接口增加 `inventory_quantity`，`inventory_price` 读取独立库存记录。来源归档复用现有机制，执行日志仅记录文件/目录哈希和汇总数。

台账按确认顺序展示库存量、库存价；个人显示设置一次迁移，保留其他列及原浏览方式、每页条数。原价比较字段仍保留，上版价格可从“显示”选回。新字段复用采购价格字段权限。

管理员命令 `procurement-inventory` 默认只读预检；执行需匹配文件哈希和完整在用目录指纹。重复同数据更新数为 0；异常整批回滚。库存更新不写入调价、历史快照、价格日期和修改人；采购价格保存及启用不覆盖库存。

- 81 项相关后端测试通过，覆盖库存导入、采购协同、价格版本、权限、迁移、CLI、备份和回退基础能力；其中 12 项为本次库存专项用例。
- 26 项前端回归、类型检查、独立候选构建、安全检查、差异格式检查通过。
- 真实正式数据库副本的隔离验收通过，逐项数据匹配、概况除库存外完全一致、20 张业务表指纹一致、重复导入不重复写入；记录在 `.scratch/inventory-acceptance-20260911/acceptance.json`。
- 桌面 1749×1272 与窄屏 390×844 实测：表头、50 条分页、搜索、零库存、缺失库存价和局部横向滚动正常。页面未出现整页横向溢出。
- 正式服务 readiness 全部正常；Chrome 已登录页面刷新后确认新资源、9 列顺序和真实库存数据，控制台无警告或错误。
- 验收时使用相同主机不同端口，内置浏览器的测试登录覆盖了原登录 Cookie；正式账号及服务器会话未改动，内置浏览器需要重新登录。后续隔离 UI 验收应使用不同主机名或独立浏览器会话，避免同名 Cookie 冲突。未修改登录机制。

## 运行与回退资料

- 正式目录：`C:\Users\gnedhy\AppData\Local\Packages\OpenAI.Codex_2p2nqsd0c76g0\LocalCache\Local\HonghaoAI\production`；入口 `http://127.0.0.1:8000/`。
- 当前服务父/子 PID 为 3140 / 8808；日志 `service-inventory-20260911.out.log`、`service-inventory-20260911.err.log`。PID 仅为本次记录，后续操作前重新核对。
- 更新前完整数据快照及原采购 Excel 归档、旧 dist、旧后端变更文件保存在 `.scratch/inventory-rollout-20260911-161918/`。`recovery-sha256.json` 逐项校验通过；`price-tables-before.json` 保留原业务指纹，`applied.json` 记录实际更新结果。
- 原生快照为上述目录下 `snapshots/snapshot-20260911T081918991955Z`；采购 Excel 单独保存在同级 `procurement-sources`，以兼容原备份工具的范围。回退需停止服务，将数据快照、原采购文件、`dist-before` 和 `backend-before` 一并恢复；保留当前失败状态以供排查，不能只回退页面。
- 候选构建 `.scratch/inventory-build-20260911`；正式入口已切至 `index-eOY5fpdG.js` / `ProcurementWorkbench-BobIlSYi.js`，沿用现有 CSS。
- UI 当前证据由 `.impeccable/review/procurement-ledger/current.json` 指向。本次没有提交或推送。
