# 采购资讯正式部署

用户先确认稳定翻页版本“可以了”，再明确授权“做进去正式版本里面吧，然后启动给我看看”。本次仅本机部署，不包含提交、推送、分支合并或清场。

## 部署结果

- 正式入口：http://127.0.0.1:8000/；health.environment=production，readiness=ready，各检查通过。启动进程25868。
- 使用原正式数据目录 `C:/Users/gnedhy/AppData/Local/HonghaoAI/production`，宿主解析为 `C:/Users/gnedhy/AppData/Local/Packages/OpenAI.Codex_2p2nqsd0c76g0/LocalCache/Local/HonghaoAI/production`；environment标记和原服务PID已交叉核对。
- 页面文件由验收通过的候选dist复制，逐文件SHA-256一致（0处不同）。后端使用当前已测试源码，不更改启用配置。
- 重启后与备份逐行对比：23张采购表及identity_users共24表完全一致，包含原账号密码校验记录。不迁入候选的演示价格、账号或资讯缓存。
- 正式服务首次自行采集成功：商务部5、生意社50、隆众20，共75条最近30天资讯。原页面保持每页5条、最多2页、按天分组和稳定高度。
- 安全检查通过。检查中发现候选QA脚本固定测试凭据，已改为从NEWS_QA_PASSWORD读取；不改真实账号密码。
- 首次启动因宿主遗留空的模块覆盖环境变量被门禁拒绝，未写正式数据；移除子进程环境变量后，按既有持久配置正常启动。未绕过门禁或重写配置。

## 备份及回退

正式目录下 `backups/news-release-20260910/` 保留：

- `snapshot-20260910T055839623814Z/`：一致性数据库和配置快照，创建时已校验。
- `procurement-sources/`：标准快照遗漏的采购来源原件补充备份。
- `previous-dist/`：替换前页面文件。
- `previous-release-source.zip`：原有上次正式源码归档副本。
- `deployed-code-and-dist.zip`：本次程序源码、依赖锁与页面文件归档，不含正式数据库或测试账号文件。

无需回退数据即可撤回页面；若撤回整个版本，先停止正式服务，使用上述旧页面与上次正式源码基线，按原production目录和持久配置重启。数据快照仅用于必要恢复，不把已产生的新业务数据覆盖回旧快照。

现有候选实例及旧验证材料保留，未清场。正式浏览器需使用原账号登录；没有创建免登录会话或重置密码。

## 同日对齐精修

后续按用户反馈收紧：共享行高100px改为80px，资讯采用22px标题行高与7px上下留白；两卡从641px缩为541px。候选构建、全套资讯交互及独立视觉检查通过，正式8000已核对新样式生效。前一版备份在 `pre-compact-dist/`；数据与业务逻辑未改动。

用户确认后，统一排行与资讯的标题区、五行和页脚位置。候选页使用浏览器内排行样例（不写数据库），验证1920px五行及页脚坐标一致，1920/900/390px翻页卡片尺寸稳定；构建与独立视觉终审通过。正式页面已更新相同构建，HTTP核对新样式命中；未重启后端、未改数据或排行逻辑。替换前页面保存在 `pre-alignment-dist/`。当前视觉证据由 `.impeccable/review/procurement-news/current.json` 指向。
