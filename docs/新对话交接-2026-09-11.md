# 当前开发交接

更新于2026-09-12。本文件只保留现行状态，9月11—12日过程已整理到[更新日志](./更新日志-2026-09-11至12.md)，详细实现见[研发调整](./研发工作台调整-2026-09-12.md)。整理前全文保存在`.scratch/closeout-20260912/documents-before/`，供核对历史部署和旧回退路径。

## 当前状态

- 项目为本地React/TypeScript + FastAPI/SQLite Web应用，采购与研发五部已启用；其余模块沿用现有配置，不扩建。Astra是开发Agent交接方向，不是应用API集成。
- 正式入口`http://127.0.0.1:8000/`，production / ready。本次应用内更新日志已补齐两天内容，前端入口`index-BlSCESF6.js`；后端无需重启。回执`.scratch/closeout-20260912/release.json`。
- 采购台账194项、研发五部分流107项、采购V21；研发65条在用完整内编，21期历史回算＋1期正式基线，3组原表试算单独保留。
- 研发金额比较先四舍五入到分，显示及悬停两位；当前最新优先成本与最近一次不同的两位价格比较，连续同价延续最后变化。日期排行比较区间首尾，双成本排行比较当前两种成本；内部公式精度保留。最终修正时53涨/4降/7平/1项无比较基数是当时快照，不是固定业务指标。
- 采购比较口径与研发不同，见[采购台账比较](./价格台账最近调价-2026-09-11.md)，不要统一成一个算法。
- 新增与编辑配方经试算、保存草稿、管理启用；草稿持久化且独立于正式核算。停用检查引用，恢复保留ID并重新试算。CF020C不可选、CF401B唯一方案、RH-9收率102%仍待业务核对。

## 验证与恢复

最终业务改动：68项研发回归、追加大值边界和3项前端金额测试通过，独立代码复审完成。此前第二轮完整后端365项、前端28项及隔离生命周期闭环通过。这些是分轮证据，不是本次文档整理重新执行全量回归。

研发最新实现证据由`.impeccable/review/research-cost/current.json`指向；本次日志页面与文档核对另存`.scratch/closeout-20260912/`，不把文档修订冒充重新完成原界面评审。历史manifest记录当时文件哈希，文档整理后的差异属于本次收尾。

最后业务变更备份及采购/研发哈希不变证明在`.scratch/research-rounded-20260912/`；本次日志更新前端备份在`.scratch/closeout-20260912/previous-frontend/`。原数据库、配置及来源备份继续保留；本次不迁移、不改业务数据、不重启。

## 接手顺序与边界

1. 读项目与上级AGENTS.md、README及本交接，再读本次所涉模块文档；旧日期记录只用于追溯。
2. 核对Git、实际数据目录及服务。此前main基线为`48f49c800c447c89c77e66912b5c6cd9984b9bfd`。用户已授权将两天源码、测试和文档提交推送，最终状态以当前Git与远端HEAD核对为准；旧基线不含这批增量。
3. 正式服务直接读取仓库`dist`，开发构建必须使用独立输出目录，验收使用隔离数据；不得在正式库写测试数据。正式目录由运行配置确认，不凭端口或旧PID判断。
4. 保留所有未提交改动、来源工作簿、冻结记录、备份及评审证据。不重复导入、补料、回算或初始化基线。
5. 收尾状态与清场候选见[知识收尾](./知识收尾-2026-09-12.md)。用户已在收尾报告后授权清场、提交及推送。旧构建已归档校验，但删除被自动审批阻止，23个原目录仍保留；未修改生成记忆。截图、配方原型和备份仅本地保留，不纳入代码提交。
