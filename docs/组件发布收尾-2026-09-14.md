# 组件统一发布与收尾

2026-09-14，用户确认替换后完成正式发布。正式前端为 `index-GqMhjhjG.js`，后端 PID 22416；使用既有 production 数据目录。发布源码基于 `443ee50` 加本轮组件统一改动；随后用户授权提交与推送，实际提交及同步状态以 Git 记录为准。

## 发布与快速核验

- 发布前备份正式 SQLite、旧前端、旧后端修改文件及配置；保留当前后端快照。正式服务重启加载新增配方原因修复，无数据库结构变更。
- 9 个发布文件的磁盘及 HTTP SHA-256 一致；readiness 全部正常，35 张采购／研发业务表与发布前逐行一致。
- 正式浏览器以林菲菲现有账号登录，检查配方台账、A-63D 详情、原因选择、单行编辑布局、取消继续编辑、确认退出、授权表及关于系统。未保存或启用正式业务修改；最终无控制台错误，停留关于系统并保持登录。
- 正式备份的隔离副本完成采购 CF150B 录价 3.50→4.50、核对启用、研发 A-63D 自动同步、蔡诗璐编辑试算与保存、重开恢复、普通编辑启用被拒绝、林菲菲启用成功。初次测试遗漏运行配置导致接口未开放，补齐副本配置后完整通过，非产品缺陷。
- 发布前类型、安全检查及独立构建通过；日志文案补充后再次通过类型检查及构建。沿用同轮 408 项接口、37 项前端测试结果，完整覆盖及限制见 [验收报告](组件统一验收-2026-09-14.md)。保留 1 条 Starlette/httpx 弃用提示。

本次正式浏览器重点检查研发与公共界面；采购写入闭环在隔离副本完成，正式采购业务未写入。正式登录已切为林菲菲，未重置任何密码或修改权限。

## 现役资料与恢复

开发从 [组件索引](components.md) 进入；原盘点明确标为历史。README、DESIGN、ADR-0009、当前交接及系统内外更新日志已对应最终实现，不恢复数字滚动。AGENTS 保持复用入口与隔离验证边界，未创建第二套规则。机器生成记忆只读。

本机证据：`.scratch/release-components-20260914-185659/`。包含 `before.db`、`previous-frontend/`、`previous-research.py`、配置副本、`released-api/`、`verification.json`、`closed-loop-result.json`。正式数据目录的 release.json 已更新为真实部署标记，旧标记在证据目录保存。

回退时先停止当前服务，恢复 previous-frontend 和 previous-research.py，再以相同正式数据目录启动并核验。数据库不自动回退；产生新业务后禁止整库覆盖。当前服务日志为证据目录下 service.out.log 与 service.err.log，必须保留。

## 洁癖状态及清场预览

| 事实面 | 状态 |
| --- | --- |
| 代码 | verified-current：已验收版本，纳入本轮提交范围 |
| 运行 | verified-current：正式文件、进程、页面、readiness 已核对 |
| 文档 | changed-and-verified：现役入口与发布状态已同步 |
| 规则 | verified-current：AGENTS 单入口；无 CLAUDE 副本 |
| 记忆 | out-of-scope：机器生成，只读未写 |
| 工作区 | pending：删除受平台策略阻止，现场留在忽略目录，不进入提交 |

仅以下本轮可再生成材料列为清场候选；用户看完清单确认后再执行：

| 对象 | 处理理由 |
| --- | --- |
| `.scratch/release-components-20260914-185659/closed-loop/` | 写入验证用正式数据副本；结果已记录，不是正式库 |
| 同目录 `frontend/` | 与正式 dist 已校验一致的构建副本 |
| `.scratch/component-unification/mobile-review/` | 手机审阅截图，确认不再复核后可移除 |

保留发布备份、当前服务日志、组件源码／示例及未提交成果。8050 预览继续保留，待复核结束；本轮不停止它。旧清场被平台拒绝的范围不重试、不通过迁移绕过。`.scratch/repository-only-check/node_modules` 是目录联接，禁止递归跟随或整体清除 `.scratch`。

用户看完清单后明确确认清场，并要求删除受阻时不上传这些材料、继续提交和推送。三项删除操作被自动审批以 `blocked by policy` 拒绝，未执行删除或迁移；不重试绕过。已核对三项均被 `.gitignore` 排除，`.scratch` 下没有 Git 跟踪文件，正式 readiness 正常。发布备份、正式日志与 8050 预览继续保留。
