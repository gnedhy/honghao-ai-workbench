# 2026-09-14 当前版本收尾报告

本报告覆盖本日已验收功能的日志维护、文档对齐、外部归档及发布复验。清场未执行，源码未提交或推送，长期记忆未修改。

## 现役文件与职责

| 文件 | 现役职责 |
|---|---|
| README.md | 启动、运行环境、业务入口和当前交接 |
| CONTEXT.md | 统一领域概念与成本核算口径 |
| PRODUCT.md | 产品范围与能力边界，不重复运行快照 |
| DESIGN.md 与 .impeccable/design.json | 现役共用视觉约定；保留相关视觉审阅证据 |
| AGENTS.md 与 docs/agents/ | 工作边界、任务跟踪与领域文档规则 |
| docs/研发受控成本调整-2026-09-14.md | 现役成本、草稿和授权机制 |
| docs/正式发布-2026-09-14.md | 已上线范围、账号及发布事实 |
| docs/当前交接.md | 下一次工作统一入口 |
| docs/history/ | 早期计划、设计结论、历史交接和验收过程 |

“关于系统”及 Markdown 日志增加 9 月 14 日六类结果，保留旧日期；不写内部路径、密码或中间返工。历史交接保留兼容入口，原开工与设计文档复制至历史目录，原件在清场确认前继续保留。

## 外部归档与恢复

专属归档目录：`E:/Work/Daily/2026-08-17_企业AI中台独立研发-归档/20260914-163805`。

首批 `历史资料与验证现场.zip` 包含 3,978 个文件；配套 `manifest.json` 记录原始相对路径、用途、大小、SHA-256，`恢复说明.md` 说明逐项恢复。全部文件已校验，抽样解压开工 Markdown、设计图片和一次性脚本验证可读与哈希一致。图片没有假称无损转换为文字；设计结论另存历史 Markdown，原始图片保留。

增量 `收尾发布与预览增量.zip` 含 582 个文件，`最终校验回执.zip` 含 2 个最终回执／日志快照；每批有单独清单及 SHA-256。三批压缩包共约 504 MiB，完整性和逐项校验通过。已抽样恢复预览一致性数据库，SQLite integrity_check 通过。预览运行中的数据库只取一致性备份，不以复制 WAL 作为恢复方案。

本轮发布现场：`.scratch/closeout-release-20260914-164550`。包含正式数据库的一致性备份、旧正式和旧预览前端、隔离闭环副本及验证结果。正式产生新业务后不得用旧数据库整库覆盖；本次前端回退只使用旧前端文件。

## 检查结果

- 模块相关 42 项通过：覆盖统一锁定、待生效即锁定、重启生效、四项核对更新和审计；修正旧测试前提，不放松权限规则。
- 前端 34 项通过；TypeScript、测试数量门禁（406 ≥ 140）、静态安全扫描和隔离构建通过。
- 全量接口 406 项全部通过，未跳过失败或降低数量门槛。仅保留现有 Starlette/httpx 弃用提醒，不影响本次通过；未新增依赖处理无关升级。
- 74 份根目录及 docs 文档的 145 个本地 Markdown 链接全部有效；现役设计 JSON 的文档哈希和源码引用有效；运行源码、脚本及测试不引用拟归档的开工／设计图片目录。历史记录中的旧临时路径通过外部归档索引恢复。
- 2026-09-14 16:50:56 正式前端发布，入口 index-D9Xsan5D.js，8 个文件与隔离构建逐一哈希一致；服务未重启、正式数据库未替换。
- 正式登录、关于系统六类日志、采购 194 项台账、研发 65 项成本台账、隐藏未启用功能模块和总经办禁用开关均在浏览器核验。正式 API readiness 全项通过，35 张采购／研发业务表与发布前一致。
- 原 gnedhy 浏览器会话刷新后失效；使用已有肖聪管理员账号只读核验，目前正式页保持该账号登录并打开“关于系统”。未重置密码、修改角色或启用任何业务数据。
- 静态安全扫描为本轮自动检查，不冒称新一轮完整数据安全审查。
- 正式数据隔离副本完成采购 CF150B 3.50 → 4.50、核对启用、A-63D 自动同步、蔡诗璐编辑收率/试算/保存、重开恢复、普通编辑启用拒绝、林菲菲启用和历史新增。成本依次为 1.954149869 → 1.959579266 → 1.989993569 元/kg；演示价格与配方不回写正式库。

## 拟清场对象（尚未执行）

所有相对路径均以项目根目录为界，执行前解析绝对路径并重新检查服务引用和归档哈希。

| 对象 | 处理建议与理由 |
|---|---|
| 开工文档/ | 19 份 Markdown 已归入历史，24 张截图已外部归档；确认后移除旧目录 |
| 设计概念/ | 6 份 Markdown 已归入历史，13 张图片已外部归档；现役 DESIGN 保留，确认后移除旧目录 |
| .scratch/ | 4,336 个非运行文件约 1.69 GiB 已归档并匹配哈希；逐项清理，保留下述两份正式运行日志及其父目录 |
| .playwright-cli/ | 49 个历史浏览器快照，已外部归档后可移除 |
| output/ | 2 个旧验证日志，已外部归档后可移除 |
| .pytest_cache/、__pycache__/、*.tsbuildinfo、空 .codex-tmp/ | 可再生成缓存；仅对本项目文件，排除依赖目录 |
| 隔离 8050 服务 | 确认后重新核验进程、端口与 research-ui 数据/前端路径，再停止；不按端口直接杀进程 |

上述五类归档原件合计 4,449 个文件，约 1.71 GiB；准确路径、数量与例外见外部归档的 `清场预览.json`，确认后执行前重新校验。

**运行例外，禁止删除**：`.scratch/release-module-lock-20260914-162553/service.out.log` 和 `service.err.log` 正在被正式服务使用，必须连同父目录保留。此轮不为搬迁日志额外重启业务服务；下次计划重启时可将输出路径改到正式数据目录下的 logs。

8050 的 research-ui 前端、数据已做外部备份，但本清场清单只申请停止该预览服务，不申请删除上级 visualizations 或其中其他材料。

正式服务、正式数据与运行配置、dist、api、src、public、scripts、tests、锁文件、.venv、node_modules、.uv-cache、.data、Git、现役文档及 .impeccable 视觉规范和审阅记录均保留。未启用功能的源码不删除。外部归档保留，不纳入代码提交。

## 六个事实面

| 事实面 | 状态 | 结论 |
|---|---|---|
| 代码 | verified-current | 当前实现及 406/34 项测试通过，模块旧测试前提已修正 |
| 运行态 | changed-and-verified | 正式日志发布并核验，业务表不变 |
| 文档 | changed-and-verified | 单一现役交接、权限说明与历史归档索引一致 |
| 规则 | changed-and-verified | AGENTS 只修正“采购与研发均已上线”的事实，引用有效 |
| 记忆 | out-of-scope | 仅按指示读取，未修改长期记忆 |
| 工作区 | pending | 已完成可恢复归档与具体清场预览；删除、停预览、提交和推送尚未执行 |

## 提交与同步交接

目前 main 与远端 main 基线均为 c2d987e，工作区包含本日及此前已验收、尚未提交的功能更新。清场确认后再复核完整 Git 差异，不把本轮整理当成只有文档的小提交。

建议提交范围：已验收采购/研发界面与权限、成本策略和草稿、排行与共享控件、账号配置脚本、相关测试、日志和现役/历史文档。排除数据库、密码、会话、日志、运行备份与截图归档。

建议提交说明：`完善研发成本调整与启用授权，统一交互并整理版本文档`。

由用户决定提交和同步；后续先检查暂存清单及差异，再提交，核对远端变化后推送 main。本轮不自动执行 Git 写入或同步。

## 清场确认边界

依 neat-freak：“只有用户看完汇报后明确确认可以清场，才执行删除”。本报告交付前保留原现场；收到具体清单的确认后，才移除已核验归档的原件、可再生成残留并停止无用预览。完成后再检查正式服务、引用和 Git 差异。

## 使用说明补充收尾

已补齐研发章节及账号启用权分配说明，同步当前交接、正式发布记录与业务更新日志。最新正式入口为 index-B6IzdmOs.js，前述 index-D9Xsan5D.js 为本日上一批日志发布。该补充只改帮助文本，类型检查、隔离构建及正式浏览器验证通过；406/34 项全量回归记录属于此前业务基线，不声称本次重跑。

新增构建、构建日志与旧正式前端备份已纳入 使用说明收尾增量.zip，配套 manifest-使用说明.json 完整校验通过。清场预览.json 已重新计数，保留两份正式运行日志的例外。无删除、停服、提交或推送。



## 清场执行状态

用户已在报告后确认清场，并提出无法删除时迁移。4,449 个候选文件已重新逐项核验与归档一致。停止 8050 与删除文件的组合请求在执行前被自动审批拒绝，仅返回 blocked by policy，无更具体原因；未执行停服、删除或迁移，也未换工具绕过。4,449 个原文件全部仍在，正式服务 ready，8050 仍运行。当前状态为“已获用户授权，但受平台执行策略阻塞”，不是等待用户再次确认。执行回执在外部归档的 清场执行结果.json。


## 仓库范围整理

按后续用户指示，旧开工／设计原目录仅取消 Git 跟踪并加入忽略，本地文件保留。历史文档纳入 docs/history，相关链接改到仓库内历史文档或归档索引。只调整暂存索引与工作区，不提交、不推送、不改写 Git 历史。仅包含仓库文件的副本构建通过，运行依赖复用本机已安装版本。验证目录的 node_modules 联接移除被自动审批拒绝（blocked by policy，无更具体原因），该联接保留且不得在后续归档／清理时递归跟随。
