# 用户管理统一权限：候选验收

本文保留 2026-09-10 当次候选验收过程；下文“最新”、账号数量及“正式切换前”均针对当时，不代表当前运行态。后续组织结构、只读我的账号和关于系统已覆盖部分界面描述。发布过程见 [系统设置正式部署](../../releases/系统设置正式部署-2026-09-10.md)，现役界面约定见 [系统设置](../../../.impeccable/surfaces/settings.md)，本轮核对见 [提交准备](提交准备-2026-09-10.md)。

## 已批准范围

采用B紧凑分栏，系统设置仅常规设置、用户管理、审计日志。账号基本资料、工作台与功能权限、账号状态分组；资料和授权分别保存。删除独立字段配置入口及请求，旧字段管理接口停用。内置字段权限跟随所属scope，读取≥2、编辑≥3；采购业务能力采用下方最新矩阵，知识资料自身边界与不可自审规则保持。

## 正式账号权限差异（只读检查）

2026-09-10以正式数据目录 `C:/Users/gnedhy/AppData/Local/HonghaoAI/production/honghao.db` 只读检查7个现有账号。仅取消旧字段策略时差异为0；随后用户批准扩大采购编辑/管理能力，最新完整差异为5个启用账号、10项业务能力变化（见下表）。没有普通采购管理账号，当前无人因此新增价格启用或目录管理能力。未读取密码或会话值。正式切换前须重新运行，避免验收期间账号变化使清单过期。

| 账号 | 姓名 | 新增能力 |
|---|---|---|
| lixueying | 黎雪莹 | 退回更新、查看已归档记录 |
| zengwenshu | 曾文舒 | 退回更新、查看已归档记录 |
| fuyong | 付勇 | 退回更新、查看已归档记录 |
| wangyuanyuan | 王远远 | 退回更新、查看已归档记录 |
| zengqi | 曾淇 | 退回更新、查看已归档记录 |

## 最新批准的采购矩阵（覆盖早期独立目录限制）

查看：读取数据和历史。编辑：再增加录价/导入/提交、退回更新、查看归档；价格启用仍可单独授予。管理：再增加自动价格启用及原料目录新增、修改编号、归档/恢复和部门范围管理。系统账号管理、启用权转授、整轮取消仍按原独立授权，不因采购管理自动开放；不可自审继续有效。

页面权限区对应当前实际模块/工作台运行状态，不以重启后待生效状态判断；未运行范围保留原授权但禁止修改。沿用常规设置折叠样式，折叠不丢未保存修改。退回与归档等既有后台操作本次只调整授权，不新增前端业务入口。

最新展示采用两个同级折叠组：功能模块（仅知识库）与职能工作台（四项），系统管理员身份在外，底部统一保存。移除外层折叠与权限预览。尚未接入人员授权的功能模块不显示，不增造后端权限模型；未启用项置灰。组间只保留一条1px分隔线，避免相邻边框叠加。

当前迁移分支的复核命令：先从受保护配置注入 `HONGHAO_DATABASE_URL`，再执行 `.venv/Scripts/python.exe -m scripts.user_access_diff`。脚本使用 PostgreSQL 只读事务，不初始化或迁移数据库；当次 SQLite 检查结果仍保留为历史基线。

## 环境边界

系统设置外框固定高度726px（取常规设置实测基准），保持原宽度；矮屏按视口减安全留白缩小。标题与分类导航固定，长内容在内部滚动，切换页面和展开折叠不改变外框。桌面/手机/矮屏尺寸及位置一致性已验证。

正式8000不重启、不更新dist、不写账号或权限。候选8036使用 `settings-candidate-20260910` 独立演示数据库，复制自旧资讯候选而非正式库；额外创建查看、编辑、管理、跨范围及停用示例账号。旧字段表及历史审计保留，不清理。

用户确认候选界面和差异清单后才可正式切换；本次不提交推送。

## 验证结果

- 后端相关回归89项、独立权限检查18项、前端既有回归26项通过；只读差异脚本测试通过。
- 最新矩阵相关后端76项回归（75首次通过、1旧断言更新后通过）及独立9项安全定向检查通过。新增实际API测试覆盖编辑退回/归档读取、管理新增/改编号/归档恢复/启用及授权降级排期；跨范围、停用、自审和转授边界仍有效。
- 浏览器闭环通过：资料与授权分别保存、重新登录核对能力、审计记录、取消/Esc关闭、取消新建、当前账号保护、停用状态、390px窄屏。
- 通过模拟响应检查加载、读取失败、空列表及保存失败保留输入；未向正式环境注入响应或写数据。
- 独立安全和UI复核无阻断发现，审计对象英文account已补为账号。候选构建、安全门禁及UI机械检查通过。
- 验收入口为 http://127.0.0.1:8036/，示例管理员test-admin；使用候选测试密码，不是正式gnedhy账号密码。
- 本轮浏览器矩阵确认编辑归档接口可读、管理自动启用与目录能力生效，且不获得转授/整轮取消。权限折叠保留草稿、按实际运行状态禁用未运行范围、父模块停用/状态读取失败/加载中禁用均通过；未改变任何实际模块运行状态。
