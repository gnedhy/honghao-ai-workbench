# 采购工作台验收收尾

后续发布授权、独立审查和补充验证以 [发布审查记录](发布审查-2026-09-09.md) 为准；本文保留提交推送阶段的核对结果，不将阶段性“待完成”误认为后续未执行。

## 当前结论

用户已验证 8020 候选验收入口，确认本轮程序可接受，授权先对比、整理，再提交与推送。以 8012 已固化程序及采购数据为基准；本次文档整理不改变运行代码。产品后续以工作台为核心，其他模块按需评估，不自动扩建或删除。

## 远程、运行态与数据对比

| 对象 | 核对结果 | 状态 |
|---|---|---|
| 远程 main | 本轮开始时为 017fa7a，不含采购分支之后的两次提交及本轮未提交改动 | verified-current |
| 远程采购分支 | 本轮提交前与本地 HEAD 同为 23a2976，无分叉；今日修改尚在工作区 | verified-current |
| 8012 | 固化验收程序；138 项在用原料、20 个价格版本；readiness 通过 | verified-current |
| 正式候选 data | 独立 production 目录，模块关闭、未启动；采购业务表与 8012 一致 | verified-current，未上线 |
| 8020 | 同一固化程序和合并数据的 test 验收副本；readiness 通过，用户已验收 | verified-current，非正式上线 |
| 旧 .data | test 旧库，137 项在用原料、16 个价格版本，不作为采购基线 | verified-current |

数据比较不混用账号凭据、用户显示偏好和实例内文件绝对路径。正式候选库继承旧库的 2 个项目、3 个会话、6 条消息、4 个任务，采购业务数据整体来自 8012；原始无价原料 322 项保持归档。

8020 验收新增一轮涉及 3 项原料的修改，状态已取消，未增加价格版本；副本中还有相应来源行及审计变化。它们不自动回写正式候选库，不能以验收后整库覆盖方式混入正式基线。gnedhy 原密码已恢复，原库密码没有修改；密码、数据库和原件不进入 Git。

## 验证与证据

- 应用源码、依赖锁文件及构建产物与固化版逐文件 SHA-256 对比一致；本轮新增差异仅为文档整理。
- 完整门禁：226 项后端测试、25 项前端测试、构建、自动安全检查通过，测试没有跳过。
- 数据恢复演练：42 张表恢复后逐表一致，完整性与外键检查通过；采购来源 Excel 由独立补充包恢复并核对哈希。标准快照不包含此 Excel，README 已明确恢复要求。
- Node 依赖审计报告 0 个已知漏洞。Python 依赖审计被系统应用控制策略阻止，保留 pending；不能把自动安全检查或测试通过当成完整数据安全审查。
- 程序 ZIP、原库与候选快照、来源原件、账号凭据和机器可读核对结果均保存在仓库外的本机迁移目录。它们是恢复资产，不上传远程仓库。

## 知识与工作区处置

| 事实面 | 处置 |
|---|---|
| 代码 | verified-current：保持验收实现，提交采购及相关账号、设置、运行支持代码和测试 |
| 运行态 | verified-current：区分 8012、8020、候选库；没有正式切换 |
| 文档 | changed-and-verified：README、CONTEXT、路线图和 ADR 对齐工作台优先及真实数据状态 |
| 规则 | changed-and-verified：AGENTS 指向当前产品边界；不改变正式发布门禁 |
| 记忆 | out-of-scope：平台生成记忆只读，未写入 |
| 工作区 | pending：本地视觉评审材料、临时演练目录与备份保留，未授权清场 |

`.impeccable` 中的历史视觉记录、截图和候选稿只保留本地复核，本轮不上传；其中日期化材料不替代本收尾记录。`.scratch`、`.codex-tmp`、失败演练副本和临时服务均不按名称自动删除或停止。后续清场必须在结果汇报后另行确认，不与本次推送混为一谈。

## 已知遗留与发布边界

- 原料详情单笔录价未沿用批量录价确认信息，高波动仍可能二次提示。本轮已冻结，不在文档收尾时悄悄修改功能。
- 全量测试有一项 Starlette TestClient 使用 httpx 的弃用警告；未擅自替换依赖，完整日志留在本机验收证据中，不影响本次测试通过结论。
- Python 依赖漏洞复核、独立代码/数据安全审查和模块降级/重新启用演练仍须在正式启用前补齐；离线恢复演练不冒充这些审查。
- 本轮授权提交并推送采购开发分支，不包含创建 PR、合并 main、部署、正式启用或清场。Issue #23 与正式启用审查要求保留，后续按相应授权推进。
