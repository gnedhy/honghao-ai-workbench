# 采购闭环测试记录

## 结论

2026-09-11，在 main 基线 `48f49c800c447c89c77e66912b5c6cd9984b9bfd` 上完成隔离自动化验证：原有后端 262 项通过，新增连续闭环 1 项通过，前端 26 项通过。未发现本次覆盖范围内的阻断故障。

测试使用临时目录、程序生成的 Excel 样本和测试账号；没有复制正式数据库或在正式库执行测试写入。此次新增测试与本文未提交或推送。

## 连续场景

新增 `tests/test_procurement_closed_loop.py` 通过真实应用 API 顺序验证：

1. 新建原料并关联两个部门，录入 12.5 的草稿价格；正式价尚为空。
2. 启用第一版，正式价变为 12.5。
3. 保存 25 的高波动报价，未确认启用返回 409，正式价仍为 12.5。
4. 核对参考价并提交确认，启用下一版，正式价更新为 25，版本号递增。
5. 部门关联和已定价计数正确；旧版仅状态由 active 转为 historical，其余快照内容保持一致；原料历史包含两次正式价格。
6. 保存下一轮 26 的报价后取消，正式价与当前版本不受影响。
7. 关闭测试应用生命周期后重新打开同一测试库，正式价、版本及历史保持一致。

首次编写场景时，将旧版本整个响应要求完全相等，误把正常的 active→historical 状态变化当成失败；已修正测试预期，未为此修改业务逻辑。

## 其他覆盖与结果

| 检查 | 结果 |
|---|---|
| 原有后端回归 | 262 passed，156.78 秒 |
| 新增连续场景 | 1 passed，5.36 秒 |
| 前端逻辑测试 | 26 passed，无跳过 |
| TypeScript 类型检查 | 通过 |
| 测试数量门槛 | 263 ≥ 140 |
| 仓库安全门禁 | 通过 |
| 独立 Vite 构建 | 通过；5 个产物逐文件 SHA-256 与现役 dist 相同 |
| 正式服务只读检查 | readiness=ready，含采购调度器在内全部检查为 ok |

已有测试同时覆盖权限拒绝及撤权、并发旧版本冲突、整批原子回退、零价与空白、重复/未知编号、排期隔离与到期启用、取消与重新确认，以及账号组织、反馈、备份恢复等模块。

原始证据：`.scratch/closed-loop-20260911.xml`、`.scratch/closed-loop-scenario-20260911.xml`。候选构建在 `.scratch/closed-loop-build-20260911/`，未覆盖正式 dist。

## 验证边界

本次闭环为 API 集成测试，前端验证为现有逻辑测试与构建，不等同于新增一轮浏览器逐按钮写入验收。此前本任务已只读检查采购四个页面、图表资讯、设置、组织账号、审计和反馈的实际加载。

仍有一项既有 Starlette TestClient/httpx 弃用提醒，不影响本次通过结果。安全门禁不等于完整代码审查或数据安全审查。本次未修改业务源码、发布新构建、重启正式服务或执行正式数据恢复。
