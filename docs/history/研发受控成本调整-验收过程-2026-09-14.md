# 研发五部账号与受控成本调整：现役说明

## 本轮实现

决策见 ADR-0009。两种成本口径分别使用 `manual_costs.latest` 和 `manual_costs.inventory`，空值或缺省表示自动核算。现有配方 JSON 和草稿快照承载调整原因、说明和成本策略，不重新导入 Excel，不变更采购价格。

核算结果保留 `auto_cost`、`manual_cost`、最终采用值 `cost`、`cost_source` 和 `difference`。递归引用采用上游最终值，自己的自动对照不套用自己的手动值。自动缺价保留异常，结构错误仍阻止试算。试算确认包含当前价格、配方、原因、手动成本和草稿版本。

保存后锁定填写；取消保存保留输入并收起结果；重新打开显示已存试算并后台核验。旧草稿进入编辑时补原因。启用时冻结编辑人、启用人及姓名，历史快照不随新价格改写。界面复用现有确认弹窗、选项菜单、负责人选择和金额样式，无数字滚动动效。

## 账号

2026-09-14 已正式部署并配置五位人员，现役状态见 [正式发布记录](../releases/正式发布-2026-09-14.md)。以下隔离数据与验证记录仅用于追溯。

隔离数据目录：`C:/Users/gnedhy/.codex/visualizations/2026/09/12/01a094ca-dffd-7ce3-a379-042d4c3e46d5/research-ui/data`。

已在此目录创建研发五部及五人账号：林菲菲 `linfeifei` 为研发管理，其余 `caishilu`、`chengyunhong`、`lubin`、`wenhao` 为研发编辑。使用系统现有初始密码机制。

`scripts/configure_rd5_accounts.py` 默认只核对，显式 `--apply` 才写入指定数据库。优先复用同名人员，登录名或身份冲突立即停止；不重置旧密码，保留其他授权范围和兼属部门。负责人候选加入活跃研发五部成员并保留历史负责人。

## 验证记录

- 研发核算与接口测试、生命周期与历史回算测试、组织测试通过。覆盖原因校验、非法金额、零金额、单／双口径、恢复自动、三层混合引用、缺价与循环引用、采购变价失效、历史不可变。
- 五人权限逐一通过接口验证；林菲菲可启用本人保存，四位编辑不能启用。已有林菲菲账号复用、重复执行不重复创建、旧密码保留均通过测试。
- TypeScript 检查、隔离目录构建、6 项前端成本分析测试通过。
- 实际浏览器检查：原因必填、其他说明必填、取消弹窗、两个入口、实时试算、零金额、保存锁定、取消保存保留输入、重开草稿恢复结果、启用核对信息。
- 林菲菲浏览器登录并启用本人修改。预览 RH-11 最新优先指定 4.8 元/kg，库存保持自动；台账和详情显示手动标记、自动 4.25 与差额 0.55，详情记录编辑／启用人均为林菲菲。仅为隔离验收示例。
- 桌面及 600px 窄屏检查完成；窄屏表格保留横向滚动，浏览器尺寸已恢复，账号保持登录。

## 交付边界

本轮功能已通过预览确认并正式部署；源码尚未提交或推送。隔离验收数据未迁回正式服务。

## 研发启用授权验收补充

隔离预览已配置林菲菲为研发启用授权管理人员，其余四人未授予独立启用权。设置复用采购展开、人员列表和授权记录；我的权限显示成本启用与启用权分配；用户管理区分管理权限自带和独立授权。两个编辑入口共用实际启用能力，停用配方仍需研发管理权限。

研发、采购回归测试及新增授权测试通过。新增测试覆盖：普通编辑授权前后、启用本人或他人草稿、撤销后事务内拒绝、管理权限不受撤销影响、停用与权限降级失效、不能转授权或提升管理身份、采购与研发授权隔离、账号资料能力返回。类型检查与隔离构建通过。浏览器已检查林菲菲我的权限、授权入口及五人初始状态，以及管理员用户管理中的林菲菲／蔡诗璐授权来源。

授权变更记录授权人、对象、内容和时间。该阶段首先验证隔离预览，后已完成正式部署与权限配置。

## 授权记录展示补充

采购和研发共用授权记录组件，默认展开最近 10 条；查看更多进入全部历史，按北京时间日期分组，每次加载 50 条。事件左侧合并操作人与对象，右侧仅保留时间；对象优先取审计记录中的姓名。查询增加 offset、limit（1–50）、total、has_more，按时间及插入顺序倒序排列。既有审计记录和授权规则不变。

分页接口覆盖 0、10、11、57 条及同时间稳定排序、姓名回退、查询权限与参数边界。独立浏览器测试副本验证 10 条摘要、50 条首批、61 条完整加载、折叠返回、长姓名及 600px 窄屏。测试记录不进入验收数据。
