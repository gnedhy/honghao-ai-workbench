# 当前开发交接

更新于2026-09-12。本文件保留现行状态，过程见[更新日志](../更新日志-2026-09-11至12.md)，界面和比较口径见[研发调整](../研发工作台调整-2026-09-12.md)。本轮改写前文档保存在`.scratch/release-ui-20260912-184132/documents-before/`。

## 当前状态

- 正式服务于18:47完成前端替换，入口`http://127.0.0.1:8000/`；实际production目录为`C:/Users/gnedhy/AppData/Local/HonghaoAI/production`，readiness全部ok。入口资源为`index-BLmaGT39.js`，回执`.scratch/release-ui-20260912-184132/release.json`。
- 本轮采购与研发统一看板排版、字号、抽屉交互、历史分类和换页器；采购涨跌排行支持原料详情。研发价格变化默认降序，筛选与显示菜单精简，详情宽表格使用800px抽屉，普通表单520px，窄屏适配。详情金额保留数字右侧单位，涨跌排行不重复单位。
- 研发台账、本期统计、产品抽屉历史比较最近一次不同的两位价格；价格历史列表和版本明细比较上期有效成本，同价显示“较上期无变化”。日期排行比较区间首尾，双成本排行比较当前两种成本。正式记录显示“成本 v22”，采购回算编号保留。
- 原采购194项、研发分流107项、采购V21，65条在用产品、21期历史回算和1期正式基线保留。数据会随业务变化，不作为固定展示值。
- 本次仅替换前端，不迁移或回算；发布前后33张采购/研发存储表逐表内容哈希一致。配方启用、停用引用检查及授权边界保持。

## 验证与恢复

本轮372项后端、34项前端测试通过；测试数量、类型检查、安全门禁及独立构建通过。保留既有Starlette/httpx弃用提醒。安全门禁不是全面数据安全审查。

验收截图与交互脚本位于`C:/Users/gnedhy/.codex/visualizations/2026/09/12/01a094ca-dffd-7ce3-a379-042d4c3e46d5/research-ui/`，8050仍为隔离验收现场。覆盖桌面/窄屏、排行对齐、抽屉动效与焦点、负责人菜单、筛选、历史分类及分页。

本次候选、旧正式前端及只读一致性数据库备份位于`.scratch/release-ui-20260912-184132/`。如需回退本次界面，先恢复`previous-frontend`资源，最后替换`dist/index.html`，不恢复业务数据库。正式服务直接读取仓库`dist`，开发构建必须指定独立输出目录。

## 接手边界

1. 先读AGENTS、README、本交接，再核对实际服务和数据目录，不能只凭端口判断正式环境。
2. 当前唯一工作树main，发布时本地和远端基线均为`c2d987e7d9de8f2548409b1d3795ee1955ccb369`。本轮增量未提交、未推送；此前推送授权不视为本轮授权。
3. 保留全部未提交增量、业务原件、冻结记录、备份和验收现场，不重复导入、补料、回算或初始化。
4. 收尾与清场候选见[知识收尾](../知识收尾-2026-09-12.md)。本轮未删除文件、停止8050或修改生成记忆。
