# 2026-09-10 提交准备核对

## 结论与范围

本轮按 neat-freak 完成项目内知识收尾、现有验证及只读清场预览；未暂存、提交、推送、重启服务或删除文件，未修改正式账号、权限及业务数据。准备范围为当前采购、账号组织、设置、反馈和界面完善，不扩大到其他项目。

本地 `main` 与查询时远端 `origin/main` 的基线均为 `d0b310354461d77bd1b0f36b546c3d1b9d61c1bb`，仅一个 worktree。本轮功能还在工作区，部分已先更新本机正式服务；远端提交不代表当前正式程序全貌。本文是本次准备快照，不是新版本发布凭证。

## 六面状态

| 事实面 | 状态 | 结论 |
|---|---|---|
| 代码 | verified-current | 当前测试、类型检查和构建通过；未提交改动保留。完整代码审查不在本轮范围。 |
| 运行态 | verified-current | 正式 health 为 production / ok，readiness 为 ready；首页及其引用的 JS、CSS 与本地 dist 一致。没有重放正式写入链路。 |
| 文档 | changed-and-verified | 修正本人资料可编辑、蓝色控件、旧启用确认及候选状态等过期说法；历史部署记录与当前约定分开。 |
| 规则 | verified-current | 沿用全局、Daily、项目 AGENTS.md；项目内三个规则引用存在，未新增平行规则或修改范围外配置。 |
| 记忆 | out-of-scope | generated-read-only；本轮未写生成记忆、知识库或全局 Skill。 |
| 工作区 | pending | 已阻止本地 output、临时目录误入提交；预览与验收现场保留，清场待汇报后确认。 |

## 验证证据

- `npm run verify`：测试数量门槛通过，后端 **262 passed**，前端 **26 passed**、无 skipped/todo，类型检查、Vite 构建及仓库安全门禁通过。
- `npm audit --json`：全依赖 0 个已知漏洞；OSV 官方 querybatch 对当前 Python 环境 32 个公开包名和版本返回 0 个漏洞匹配。仅上传包名和版本，不包含业务数据。新增 Playwright、Pillow 版本及许可证已补入 [依赖审查台账](./security/依赖审查台账.md)。
- 正式页面：`index-DgxcHyPc.js`、`index-VByCMAdD.css` 的 HTTP 内容 SHA-256 与本地构建一致，首页文本一致；构建另含 `ProcurementWorkbench-BZI2Taf8.js`。
- `git diff --check` 无空白错误。现有 Windows LF/CRLF 转换提示保留，没有进行全库换行重写。
- 已知非阻断警告：Starlette TestClient 对 httpx 的弃用提醒，后端测试仍全通过。本轮未为消除警告临时替换测试依赖。

以上不是完整代码审查或数据安全审查的替代证明，也没有重新执行所有页面、全部状态的独立视觉终审。旧截图和 manifest 只覆盖其对应构建；尤其旧 `current.json` 不应被当成本次完整 UI 验收凭证。

## 本轮对齐内容

- README 明确正式入口、未提交工作区与远端基线的差别，补充只读我的账号、组织归属、反馈及启用确认说明。
- [系统设置约定](../.impeccable/surfaces/settings.md) 和 [我的账号约定](../.impeccable/surfaces/profile.md) 替换逐轮矛盾叙事；组织、关于系统 brief 同步最新决定。
- [部门组织](./部门组织与人员归属-2026-09-10.md) 改为姓名与部门均由管理员维护；旧构建标为历史。
- [启用确认](./启用确认简化-2026-09-10.md) 与 [启用审查清单](./security/模块启用审查清单.md) 对齐“不手填 Issue/PR、有效记录可复用”，仍保留四项审查及后端校验。
- 界面更新日志已覆盖我的账号、帮助、初始密码、组织、反馈、关于系统、设置权限、采购资讯、台账分流、滚动和一致性共11项。左侧日期下 `23:44 整理更新` 是当轮日志整理时间，不是逐项上线时间；本次纯文档收尾不改该时间。

## 提交边界与建议

建议提交题目：`feat: 完善采购工作台、账号组织与系统交互`。本轮多项功能共享 API、设置组件、类型和样式，提交时将相关源码、测试与锁文件一起纳入，不按截图轮次拆出无法构建的中间版本。

纳入范围：当前 `api/`、`src/`、`tests/`、`tests_ui/` 的功能变更；三个新增维护脚本 `procurement_news_browser.mjs`、`procurement_news_probe.py`、`user_access_diff.py`；package/uv 依赖清单及锁文件；README、CONTEXT、`.gitignore`；本轮相关 `docs/` 文档和 `.impeccable/surfaces/` 文字约定。

不纳入范围：`output/` 的一次性账号脚本、候选构建、服务日志、测试脚本及截图；`.impeccable/mocks/`、`.impeccable/review/` 原始现场；依赖、dist、数据库、来源原件、备份、环境配置及凭据。output 已加入忽略，但原始设计证据未 blanket ignore 或删除，仍需按路径明确排除，不能使用 `git add .`。

实际提交时应按上述路径暂存，复核暂存文件名单及差异；推送前再次查询远端，若基线改变先处理差异。用户当前仅要求准备，本轮不代为提交、推送或创建 PR。

## 清场预览与未完成项

- `output/`：盘点约90个文件、4.4 MB，包含候选页面、日志与一次性维护脚本；暂不提交，保留供复核。需先判断是否仍为候选服务依赖，不能直接整目录删除。
- `.impeccable/mocks/` 与 `.impeccable/review/`：合计约336个文件、18.0 MB，含旧方向和各轮验收证据；只将已被替代且没有唯一证据的材料列为后续清理候选，保留最终验收依据。
- 旧数据目录、候选运行服务、正式备份与来源原件本轮未删除、停服或迁移。其清理需要单独确认精确对象及依赖，不计为本次已完成。
- 提交、推送、完整代码/数据安全审查、全状态视觉复核为本轮范围外；清场为 pending。复核现场仍保留，等待用户看完汇报后明确确认。
