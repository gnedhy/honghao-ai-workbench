# 系统设置正式部署

## 关于系统页面更新

2026-09-10 用户明确要求“更新到正式版”后，已将验收的关于系统页面构建更新至正式 8000。包含产品名称、当前版本与环境、左日期纵向更新日志，并移除常规设置环境提示。所有候选构建文件与正式副本 SHA-256 一致，HTTP 首页一致，production/readiness 检查通过。未重启后端，未写账号或业务数据。替换前页面保存在正式数据目录 backups/about-release-20260910/previous-dist；本次不提交或推送。

用户验收候选版本后授权“更上去”，随后要求打开正式版本。本次仅更新本机正式服务，不包含提交、推送或清场。

- 正式入口：http://127.0.0.1:8000/。更新后 health.environment 为 production，readiness 全项通过。
- 页面使用 output/settings-candidate/dist 的已验收构建，HTTP 返回的首页与候选一致；后端重启加载当前已验证源码。
- 发布前重新检查正式账号权限差异，与此前确认的 5 个账号、10 项采购业务能力变化一致。权限、访问级别、个人资料和采购范围权限相关回归通过，安全检查通过。
- 更新后将正式库与发布前快照逐行对比，23 张采购表及 identity_users 共 24 表无变化。没有迁入候选测试账号或数据，未修改正式模块配置。
- 备份位于正式数据目录 backups/settings-release-20260910：snapshot-20260910T110435665176Z 为已校验数据快照，previous-dist 为旧页面，previous-source-code.zip 为原源码归档副本，deployed-source-and-previous-dist.zip 保存当前源码与替换前页面，service.out.log/service.err.log 为新服务日志。

回退页面可使用 previous-dist；需要整版回退时，结合上一版发布归档恢复源码并按原 production 数据目录重启。不要直接用旧数据快照覆盖发布后新产生的业务数据。
