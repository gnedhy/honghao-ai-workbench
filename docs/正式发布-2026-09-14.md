# 2026-09-14 正式发布

本日后续组件统一发布、当前构建与服务及恢复证据见 [组件发布收尾](组件发布收尾-2026-09-14.md)；下文保留此前发布过程。

用户确认隔离预览后授权更新正式服务，并要求采购录价至研发同步编辑的完整快速闭环。

正式数据目录：`C:/Users/gnedhy/AppData/Local/HonghaoAI/production`；服务地址：`http://127.0.0.1:8000/`。本次上线已验收的配方比例与编辑交互、草稿试算恢复和锁定、调整原因及手动成本、研发独立启用授权及采购/研发共用授权记录展示。

研发五部新建林菲菲、蔡诗璐、成运洪、卢彬、温豪五个账号，沿用初始密码机制。林菲菲为研发管理权限及启用授权管理人员，其余四人为研发编辑权限，未配置独立启用授权。系统管理员保持原权限。

## 快速闭环

基于正式数据库备份的隔离副本，通过实际 HTTP 接口完成：采购 CF150B 价格 3.50 → 4.50、核对启用、研发 A-63D 成本 1.954149869 → 1.959579266 自动同步、蔡诗璐修正收率为 96% 并试算保存、重开恢复试算、普通编辑启用返回 403、林菲菲启用成功，最终成本 1.989993569，历史新增且草稿清除。测试价格和配方未迁回正式库。

正式服务 readiness 所有检查通过；五个账号登录及实际编辑/启用/授权管理能力、授权查询访问控制通过。上线前后既有采购与研发业务表逐行比较一致。安全扫描通过；本轮未重复全量测试，复用隔离验收阶段的相关接口测试、类型检查和构建结果。

## 发布与恢复证据

证据目录：`.scratch/release-rd5-20260914-152445/`。含停服前数据库 `before.db`、旧前端 `previous-frontend/`、本次后端快照 `released-api/`、账号配置及闭环/正式检查结果。正式前端来自验收构建，入口 `index-DwrPdfr0.js`。

数据库备份仅用于核验或有控制的恢复；正式服务产生新数据后不得整库覆盖。旧前端备份可恢复界面，但完整后端回退需结合此前版本源码，不应把本次 `released-api` 当作旧版本。当前改动未提交或推送。

## 模块与排行最终状态

未启用的功能模块（当前或待生效状态不是 active）对所有管理员统一锁定并从设置隐藏，包含 gnedhy。总经办工作台开关统一置灰、不可操作；后端同样拒绝写入。已启用模块及其它职能工作台沿用原规则。

研发涨跌排行和双成本差异排行已复用采购自动换页及指示器；双成本差异说明改为当前两种口径的成本差额。总经办肖聪、刘妍妍、张晓英已配置系统管理员权限。

本日完整收尾复验、前端日志更新及新的恢复位置见 [收尾报告](收尾报告-2026-09-14.md)。原发布证据已纳入项目外归档，原路径在清场批准前仍保留。

## 使用说明补发

2026-09-14T17:04:46.895368 补发研发工作台与账号启用授权使用说明，正式入口 index-B6IzdmOs.js。8 个发布文件核验一致；仅更新前端，无服务重启或业务库替换。恢复备份与校验清单已纳入外部归档，见本轮收尾报告。
