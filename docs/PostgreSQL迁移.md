# PostgreSQL 迁移进度

## 已确认边界

采用 Windows 本机 PostgreSQL 18 服务；2026-09-15 经用户确认，隔离验收后已完成正式切换。当前运行事实见 [正式发布记录](正式发布-2026-09-15.md)。新版本不长期支持 SQLite，旧版及 SQLite 备份用于恢复。保留账号、权限、全部业务数据、金额精度、历史、页面和操作；不同时开发 AI、智能体或多实例能力。以下第一至五步保留各步骤当时的证据，不作为当前部署位置。

| 步骤 | 内容 | 状态 |
| --- | --- | --- |
| 1 | 固定版本、数据、依赖与恢复基线 | 已完成并验证 |
| 2 | PostgreSQL 服务及数据库基础 | 已完成；服务、权限、实库与恢复检查通过 |
| 3 | 后端分批适配与对应测试 | 已完成；完整回归及收尾专项通过 |
| 4 | 全量迁移演练与差异核验 | 已完成；58 表与 78 组接口核验通过，仅隔离验收库 |
| 5 | UI、业务、并发、备份恢复验收 | 已完成；476 项后端回归、浏览器闭环与完整恢复通过 |
| 6 | 用户确认后正式切换及收尾 | 正式切换、核验与归档完成；旧实例和验收库已移除，文件删除受阻并保持 Git 忽略 |

## 第一步基线

2026-09-14，源代码为 `961f2020c0bbcc95e2af46584ffd3ae18abddf0c`。独立工作目录为原项目同级的 `2026-08-17_企业AI中台独立研发-postgresql-migration`，分支 `codex/postgresql-migration`。新建独立 `.venv` 和 `node_modules`，依赖按锁文件安装；当前服务引用的源码、依赖、dist、配置未修改。

正式实例仍使用原项目及 `C:/Users/gnedhy/AppData/Local/HonghaoAI/production`，端口 8000、进程 22416；旧预览 8050、进程 15280 均保留。端口不作为环境身份依据。

本工作目录 `.scratch/postgresql-step1/` 保存以下本地证据，全部被 Git 忽略：

| 材料 | 用途 |
| --- | --- |
| `baseline.json`、`table-baseline.json` | 快照位置、依赖版本、文件 SHA-256、58 张表的结构／数量／内容／主键／顺序校验值 |
| `snapshots/` | 现有备份工具生成并验证的 SQLite、环境配置及知识文件快照 |
| `supplemental/` | 采购来源原件、额外文件及部署标记；弥补现有备份不含采购 Excel 的范围 |
| `source.bundle`、`frontend/` | 可恢复源码历史与实际运行前端；Git bundle 完整性已验证 |
| `permission-baseline.json`、`api-baseline.json` | 14 个原有账号权限、78 组接口响应，含 65 个产品详情、成本和历史及接口返回顺序 |
| `restored-data/` | 已启动的恢复演练副本；不能作为原始快照，也不能复制回正式库 |
| `restore-verification.json`、`runtime-baseline.json` | 恢复结果与原服务运行边界 |
| `rebuild/`、`restored-detail.png` | 隔离依赖重建前端、恢复页面证据 |

数据库快照 30,724,096 字节，共 58 张业务／系统表、13,227 条记录。完整性及外键检查通过；恢复后、路径重定位前逐表校验一致。原库有 66 条配方实体，其中 65 条普通产品配方；没有把内部复配实体误算为丢失产品。

两份采购来源记录使用旧 Codex 宿主映射绝对路径；原路径和当前 production 目录对应文件的 SHA-256 均与数据库记录一致。恢复副本将这两条路径指向自身原件，只改副本。此项是后续迁移需要保留的显式路径映射，不在本步骤改写原库。

恢复副本先以测试客户端验证启动、权限与接口，然后作为独立 HTTP 服务在 `http://127.0.0.1:8060/` 启动，进程 23180。复用备份中的会话进入页面，另通过已有账号密码验证登录。浏览器确认 65 项成本台账及 RH-11 详情（最新优先 4.25、库存优先 4.11）；测试管理员创建与登录审计只发生在恢复副本。

## 验证与复现

前端 37 项测试、类型、安全检查及隔离构建通过。接口全量运行 407 项通过、1 项失败；唯一失败是父子进程编码不一致，统一启动编码后 CLI 专项 5 项全部通过，包含该失败用例，408 个用例均已有通过结果。未将分轮结果写成一次全绿运行，原始结果分别保存在 `tests.xml` 和 `cli-utf8-tests.xml`。

Windows 测试启动需设置 `PYTHONUTF8=1`，保证测试启动的子进程也使用 UTF-8；仅父进程传 `-X utf8` 会造成中文输出解码不一致。未更改测试断言或业务代码。保留原有 Starlette/httpx 弃用提示。

最终边界检查见 `final-boundary-check.json`：454 个归档文件校验通过，正式库 58 张表内容未变化，正式前端及配置未变化，原项目 Git 工作区干净。原服务与恢复服务均 ready，浏览器未发现控制台运行错误。新目录仅增加本文和交接入口，未修改业务源码、安装 PostgreSQL、提交或推送。

以下是第一步当时的恢复副本启动记录，仅适用于冻结的 SQLite 版本。第三步代码已改为 PostgreSQL，不能在当前分支照此重启；需要重建该副本时，应先从 `source.bundle` 在另一空目录恢复冻结版本和依赖：

```powershell
$env:PYTHONUTF8 = '1'
$env:HONGHAO_ENVIRONMENT = 'production'
$env:HONGHAO_DATA_DIR = Join-Path (Get-Location) '.scratch/postgresql-step1/restored-data'
.\.venv\Scripts\python.exe -m api.cli serve --port 8060 --dist .scratch/postgresql-step1/frontend
```

运行前检查端口与数据目录，已有恢复实例时不要重复启动。该副本环境标记沿用备份的 production，仅用于恢复验证，不等于现役正式库。全新 SQLite 恢复须使用冻结版本的 `restore_snapshot`，再复制 supplemental 附件并按基线校验、重定位来源；当前 PostgreSQL 恢复入口不接受 SQLite 快照。不要覆盖正在运行的目录。

后续步骤在此独立分支继续。第一步不安装 PostgreSQL、不切换数据库、不修改正式服务，不自动提交或推送。原始快照、源码包和文件清单须保留，验收副本含测试活动，应与原始基线分开使用。

## 第二步进度

以下为第二步完成时的记录；第三步后的当前状态见文末。

2026-09-14，用户确认隔离验证及正式切换边界后授权继续。所有代码改动仍在本迁移分支，原项目和正式服务未替换。

### 已准备的数据库基础

- 增加并锁定 `psycopg[binary] 3.3.5`；只更新迁移目录的依赖。
- `api/postgres.py` 提供明确的 PostgreSQL 连接、只读事务、写事务及版本化迁移入口。写事务使用同一事务级 advisory lock，后续业务权限检查和数据写入须使用这条连接。当前没有业务消费者接入，不能据此声称权限并发已通过验证。
- 迁移文件位于 `api/migrations/postgresql/`，记录版本、文件名和 SHA-256；禁止修改已应用的迁移、跨环境继续迁移及在无迁移记录的非空库初始化。迁移失败时整个事务回滚。SQL 固定 LF 换行，避免跨系统 checkout 改变校验值。
- 数据库环境标记与应用运行环境分别检查。数据库预留 `development`、`test`、`acceptance`、`production` 四种身份，DATA_DIR 继续承载文件和运行配置。
- 就绪检查覆盖连接、数据库环境、迁移历史和应用账号权限；应用账号不能具有超级用户、建库、建角色、复制、绕过行权限或修改结构／迁移记录的能力。
- 第三步尚未适配业务存储。为防止误用，当前迁移分支只要配置了 `HONGHAO_DATABASE_URL`，旧业务入口就会明确拒绝启动业务，并通过现有不可用页面返回 503；不会忽略 PostgreSQL 配置后写入 SQLite。此临时边界在第三步接入真实存储时替换，不是长期双数据库方案。

显式操作入口如下。连接凭据由本机受保护配置注入环境变量，不能写进文档、命令参数或 Git：

```powershell
$env:PYTHONUTF8 = '1'
# 先为当前进程注入对应的 HONGHAO_DATABASE_URL。
# migrate 使用迁移账号；doctor 使用仅有业务读写权限的应用账号。
.\.venv\Scripts\python.exe -m api.postgres migrate --environment test
.\.venv\Scripts\python.exe -m api.postgres doctor --environment test
```

### Windows 安装与重试记录

官方 PostgreSQL 页面和 EDB 下载入口当前提供 PostgreSQL 18.6。先下载官方二进制包，再通过官方签名安装程序尝试标准安装；两种方式均在执行 `initdb.exe` 时遭 Windows Code Integrity 拒绝。安装程序签名有效、签发主体为 EnterpriseDB Corporation；程序通过签名核验不代表系统允许其中每个程序运行。

用户要求再试后，于 21:55 显式确认管理员令牌，再执行已安装 `initdb.exe --version`，此次成功；21:57 完成空目录初始化及服务注册。上次标准安装脚本也检查过管理员令牌，因此此前失败不能简单归因于没有提权。原事件明确为应用控制拒绝，程序本身无独立签名；本次放行的具体原因日志未说明，未据此断言安全设置曾被用户改变。本轮未调整系统应用控制策略。

现已运行 PostgreSQL **18.6**：程序目录 `C:/Program Files/PostgreSQL/18`，数据目录 `C:/ProgramData/HonghaoAI/postgresql18`，服务名 `HonghaoPostgreSQL18`，账号 `NT AUTHORITY/NetworkService`。仅监听 `127.0.0.1:5432`，使用 SCRAM、UTF-8、C locale、UTC，数据校验和已开启。服务自动启动配置与服务重启验证通过；未重启整台电脑。

拦截来自 Windows Code Integrity 事件 3077／3033（Policy ID `{0283ac0f-fff1-49ae-ada1-8a933130cad6}`），不是用户拒绝切换正式环境。未关闭、调整或绕过应用控制策略，也未用其他数据库代替约定的本机 PostgreSQL。

本机 `.scratch/postgresql-step2/` 保存安装来源及 SHA-256、签名和拦截事件、初次安装失败与本次完成记录；不能只读旧 `install-result.json` 判断当前安装状态，应查 `service-result.json`、`restart-verification.json` 及实际服务。`private/` 仅当前 Windows 用户、SYSTEM 和 Administrators 可读写，不进入 Git。原始二进制解压目录 `C:/Users/gnedhy/AppData/Local/HonghaoAI/postgresql18` 保留在本机，未作为运行服务使用。

长期凭据已校验复制至 **`C:/ProgramData/HonghaoAI/postgresql-credentials`**，其 ACL 仅包含当前用户、SYSTEM、Administrators。`cluster.json` 用于集群维护，`databases.json` 保存四套数据库的迁移／应用连接；不要在日志打印内容或把文件放入代码仓库。此目录和实际 PostgreSQL 数据目录均属于持续使用的材料，不在临时清场范围。

官方来源：[Windows 安装入口](https://www.postgresql.org/download/windows/)、[支持版本](https://www.postgresql.org/support/versioning/)、[EDB 安装参数](https://www.enterprisedb.com/docs/supported-open-source/postgresql/installing/command_line_parameters/)。

### 已建立的环境与权限

| 用途 | 数据库 | 账号边界 |
| --- | --- | --- |
| 开发 | `honghao_development` | 独立 migration / app 账号 |
| 测试 | `honghao_test` | 独立 migration / app 账号 |
| 验收 | `honghao_acceptance` | 独立 migration / app 账号 |
| 正式预留 | `honghao_production` | 独立 migration / app 账号；尚未接入正式服务 |

四套均已用迁移账号应用基础迁移，应用账号就绪检查通过；迁移账号只管理所属数据库结构，应用账号仅有业务表读写、序列使用及迁移元数据只读权限。数据库拒绝 PUBLIC 连接，默认 postgres/template1 也不允许这些账号进入，未互相授予权限。

这些是数据库连接账号，不是系统中的人员账号。四库当前均没有业务表或业务数据，未复制正式账号或采购数据。完整业务结构在第三步适配，全量迁入仍在第四步，正式切换仍需用户验收确认。

再次运行实库测试时，在当前进程从长期凭据文件注入 `HONGHAO_TEST_DATABASE_URL` 和 `HONGHAO_TEST_MIGRATION_URL`，执行 `python -m pytest tests_postgres`。这组测试不使用模拟 SQLite，不隐式跳过，仅允许专属本机 `honghao_test`。

第二步已完成，第三步尚未开始。不提交、不推送、不清理初次安装和恢复验证现场。

### 本轮验证结果

完整接口回归 **417 项通过**（`api-tests.xml`）；随后补充连接异常用例，配置专项 **10 项通过**（`config-final-tests.xml`）。当前默认收集 418 项，包含原有 408 项及新增 10 项，均已有通过结果；没有将分轮结果写成一次 418 项全绿。保留原有 Starlette/httpx 弃用提示。

安全扫描、测试数量门禁、Python 编译、Git 差异检查与两份交接文档链接检查通过。前端没有改动，本轮未重复构建或发布。`tests_postgres/` 的 **7 项真实 PostgreSQL 集成测试通过**，覆盖精度／二进制、提交／回滚、只读事务、应用权限、迁移幂等／篡改／失败回滚及并发锁释放，证据为 `postgres-tests.xml`。

`foundation-verification.json` 记录四库就绪、40 次跨库访问全部被拒绝、错误密码拒绝，以及实际 `pg_dump`／`pg_restore` 恢复通过。临时恢复库验证了中文、高精度金额文本、256 字节二进制、迁移时间和校验值、应用访问权限，随后移除本次创建的恢复测试库与探针表；只剩上述四个空业务库。`foundation-backup.dump` 保留恢复证据，其中没有正式业务数据。这是同一集群内基础库恢复验证，跨服务器全量业务恢复仍在后续步骤验收。

`restart-verification.json` 记录服务重启成功、进程更换、Auto 启动模式及服务账号；重启后四库再次就绪，业务表仍为零。

`boundary-check.json` 及重试后的 `final-boundary-check.json` 确认正式库 58 张表内容与第一步快照一致，正式配置／前端及原项目工作区不变，8000、8050、8060 均 ready。凭据目录 ACL 仅包含当前用户、SYSTEM、Administrators，已确认凭据与安装证据被 Git 忽略，安装日志没有生成密码的明文。第一轮未完成摘要保留在 `step2-result.json`，最新完成摘要见 `step2-completed.json`。

## 第三步：后端原生适配

2026-09-15，用户授权继续第三步。本轮只改隔离迁移目录，正式版本、SQLite 数据、依赖和前端保持原位置。设计依据见 [ADR-0010](adr/0010-PostgreSQL原生存储与隔离迁移.md)，部署命令见 [运行与维护](运行与维护.md)。

### 本轮实现

- 核心会话、账号、权限、组织、知识、反馈、采购、研发及相关命令改为原生 psycopg 参数化 SQL。业务运行不再连接 SQLite，旧备份脚本仅作离线历史工具保留。
- `0002_business_schema.sql` 建立完整 58 张表、索引和派生视图，保留原主键、字段及外键约束；金额继续用文本与 Decimal，JSON、时间及密码二进制不改表示。显式 `_order` 承接旧插入顺序，第四步导入时还须填入原 rowid 并校准序列。
- 写事务先取得统一写锁，再在同一连接复查权限及执行写入；只读事务使用一致快照，嵌套业务调用复用连接和原生保存点。未增加 ORM、双后端、运行时 SQL 翻译或额外工作进程。
- 启动只验证迁移摘要、7 项业务版本和必要表／索引／视图。缺连接、跨环境、旧结构或权限过大的数据库账号均拒绝业务服务，不自动建库、修表或降级到 SQLite。
- 服务与恢复按数据库及文件目录互斥。关机等待正在执行的后台核算；数据库运行锁连接失效后旧服务拒绝继续操作，等待写锁或尚未提交的业务写入也须重新核验。
- 备份使用 PostgreSQL 原生转储，覆盖数据库、配置、知识文件及采购原件；恢复到同环境空库与空目录，原子恢复表和序列，明确重定位采购原件，再核对逐表及文件校验值。失败时回滚，结果不确定时保留标记并阻止启动。

第三步结束时，仅 `honghao_test` 应用业务结构并用于生成测试数据；另外三库仍为空基础库，当时尚未迁入原有人员账号、采购历史或研发数据。测试账号、临时恢复库、转储和日志均为本机隔离材料，不进入 Git。第四步完成后的状态见下节。

### 验证入口与证据

使用受保护凭据向当前进程注入以下变量，不在命令参数写密码：

| 变量 | 用途 |
| --- | --- |
| `HONGHAO_TEST_DATABASE_URL` | 本机 `honghao_test_app` |
| `HONGHAO_TEST_MIGRATION_URL` | 同库 `honghao_test_migration`，测试重建结构 |
| `HONGHAO_TEST_CLUSTER_URL` | 本机维护连接，仅创建／移除随机命名的临时恢复测试库 |
| `HONGHAO_PG_BIN` | PostgreSQL 原生工具目录 |
| `PYTHONUTF8=1` | Windows 父子进程统一编码 |

完整后端检查为 `python -m pytest tests tests_postgres`。测试仅接受明确的本机专用测试库，逐例重建其 public 结构，整个测试进程独占测试锁；不要并发运行或指向业务库。测试结束后可把专用测试库重置为空业务结构，验收与正式库保持不动。

本机证据在 `.scratch/postgresql-step3/`：采购专项、原生恢复专项、完整回归 XML、隔离前端构建，以及最终正式边界核验。首次采购专项为 129/130 通过，唯一失败是首次导入响应未得到预期数据；独立复验通过，保留原失败结果并补响应断言，最终完整回归仍须覆盖该用例，不把单项复验解释成已证实原因。

完整后端回归 **449 项通过**；随后完成最后的退出、断连、撤权和恢复保护修正，关联实际接口与新增用例专项 **182 项通过**，两轮均无失败、错误或跳过。两轮合计覆盖当前 **457 个不同用例**，不是一次 457 项运行；记录分别为 `all-tests.xml`、`final-regression.xml`。保留原有 Starlette/httpx 弃用提示。首次采购偶发失败在完整回归中未再出现。

前端 **37 项测试**、类型检查、隔离构建、安全扫描、测试数量门禁、Python 编译及 Git 差异检查通过。未修改前端源码或替换正式 dist。34 个相关文档链接有效；运行方式、角色权限、原生备份恢复和现役交接已同步。

`final-boundary-check.json` 确认正式库 58 张表内容、正式配置、前端及原项目工作区均未变化，8000、8050、8060 的原服务仍 ready。仅测试库为业务结构版本 2，已重置为空业务结构；其他三个 PostgreSQL 库仍为版本 1 的空基础库，四库应用账号权限边界保持有效。中断测试留下的一个随机恢复库经核实归属、测试身份、样本账号及无连接后移除，现场说明见 `orphan-test-databases.json` 与 `orphan-test-cleanup.json`，没有遗留恢复测试库。

第三步仅完成后端适配与样本回归，不代替下节的全量数据核验。正式切换仍须完成后续验收并经用户确认；未提交、推送或执行正式清场。

## 第四步：全量迁入与差异核验

2026-09-15，经用户确认，将第一步冻结的 SQLite 快照迁入 `honghao_acceptance`。仅该验收库应用 `0002` 并接收原有数据；附件与配置位于本工作目录 `.scratch/postgresql-step4/acceptance-data/`，应用运行标记保留原来的 `production`，数据库身份为 `acceptance`。该标记保留用于校验原配置，不表示切换了正式服务。`honghao_production`、`honghao_development` 仍为第一版空基础库，原 SQLite 服务、配置与 dist 不变。

### 迁入结果

- **58 张表、13,227 条记录**：与冻结基线逐表核对字段、数量、全部内容、主键及顺序，全部一致。原 `rowid` 保存到 `_order`；59 个自增序列已校准，保留 SQLite 自增主键的历史高水位。外键、唯一、检查及非空约束全部有效。
- **14 个原有账号**：账号、二进制密码材料、部门、角色、授权范围及采购／研发实际能力全部一致。用原有账号登录成功，未新建账号或重置密码。
- **文件**：复制环境标记、两份运行配置、原部署标记及两份采购 Excel。原件 SHA-256 与数据库记录一致，只有两条采购原件绝对路径按明确映射改为验收目录。实际库没有知识来源／版本或反馈图片；知识 PDF、Markdown 和图片二进制迁入另用合成样本验证，不能把空表核验当成实物验证。
- **78 组接口**：77 组响应完全一致，包括全部 65 个普通产品详情、成本、历史及返回顺序；`readiness` 仅将旧 `research_schema` 检查合并到结构版本检查，并增加数据库身份及应用角色检查，结果均正常。采购与研发内容无差异。
- 启动、登录和只读接口验收后，只有登录会话及登录审计表发生预期变化，业务、历史和账号表未变。启动前已生成并完整校验 PostgreSQL 原生快照，留供第五步恢复演练。

### 工具与复现

新增离线工具 [migrate_sqlite_to_postgres.py](../scripts/migrate_sqlite_to_postgres.py)，只接收第一步冻结清单及其快照／补充原件；应用运行时不加载该工具。复用原生事务、运行锁、空库检查和受控暂存目录，无 ORM、临时 SQL 翻译层或新增依赖。

先以迁移账号完成空目标库的显式结构迁移，配置受保护的 `HONGHAO_DATABASE_URL`，确认对应数据库身份与独立附件目录。以下示例仅针对验收环境，不能指向现有正式目录：

```powershell
$env:HONGHAO_DATABASE_ENVIRONMENT = "acceptance"
$env:HONGHAO_ENVIRONMENT = "production"
$env:HONGHAO_DATA_DIR = "D:\HonghaoAI\acceptance-data"
# HONGHAO_DATABASE_URL 由受保护配置注入验收库迁移账号。
python scripts/migrate_sqlite_to_postgres.py --baseline "D:\migration-baseline\baseline.json"
# 只读预检通过后，显式执行；仅允许空目标库及空附件目录。
python scripts/migrate_sqlite_to_postgres.py --baseline "D:\migration-baseline\baseline.json" --apply
```

冻结清单中的 `snapshot` 路径须位于清单同级目录内；跨机器携带基线时只调整该位置，保留文件校验值及原件映射。工具再次提交时拒绝覆盖已迁入的库。整批写入共用一个事务，不禁用数据库约束；提交前与提交后均核对完整内容。失败且确认回滚时撤回本次复制文件，提交结果不确定时保留附件和 `.restore-incomplete`，禁止启动。

核验后用同库 **app** 账号启动。登录与会话验证属于验收副本中的预期写入；全部业务写入闭环、浏览器 UI、并发及完整恢复演练留在第五步。

### 检查与证据

迁入与备份恢复相关接口测试 **37 项通过**，包括损坏／越界／缺失文件、空库／权限／环境限制、原生外键失败回滚、重复迁入、文件竞争、运行锁丢失、提交后异常保留现场、金额精度、字节内容、原顺序及序列续号。前端 **37 项通过**，类型检查、安全扫描、测试数量门禁及隔离构建通过；前端源码和后端业务实现未因第四步改变。

本地 `.scratch/postgresql-step4/` 保存 `import-preflight.json`、`import-completed.json`、`data-verification.json`、`api-comparison.json`、`migration-regression.xml`、`native-snapshots/`、`final-boundary-check.json` 和 `step4-completed.json`。接口响应与业务文件只保存在被忽略的本地目录，不进入仓库。首次合成测试发现 Windows 下 SQLite 连接未显式关闭，已修正资源释放并完成整组复验；未影响冻结源或正式服务。

第四步完成时保留只读验收库与启动前快照。第五步的写入与浏览器验收使用另建副本，见下节。

## 第五步：隔离业务与恢复验收

2026-09-15，先使用第四步原生快照恢复独立副本完成写入闭环，再将交付页 `http://127.0.0.1:8061/` 对齐当前正式业务状态。现用库为 `honghao_acceptance_step5_review_1767e4356a`，目录 `.scratch/postgresql-step5/clean-review/data/`；此前包含测试记录的 UI／恢复演练库已经退役。数据库身份为 `acceptance`，运行配置保留来源的 `production` 标记，不代表正式环境。第四步 `honghao_acceptance` 继续保留。

### 已验证结果

| 范围 | 结果 |
| --- | --- |
| 浏览器业务闭环 | 原采购账号录入 CF005 价格 17.5 → 18.125，保存并确认启用；A-63D 最新优先成本自动更新，库存优先不变 |
| 研发编辑与启用 | 蔡诗璐选择原因、调整比例，投料联动并实时试算；保存 2.123456789 元/kg 手动成本后锁定；取消保存保留输入并关闭试算；林菲菲核对原因、差异和范围后启用 |
| 草稿与重启 | 重开先显示保存结果，后台核验完成前禁用启用按钮；中止并重启本轮隔离服务后，草稿、试算与全部业务表一致，原会话仍可用 |
| 引用、精度与时效 | K172-C 最新口径手动指定 0、库存口径指定 4.123456789123456789，按口径传递下游；采购再次更新为 19.25 后保留手动值并更新自动对照／差额；旧确认返回 409，重新试算后可恢复自动 |
| 权限与历史 | 普通编辑启用请求被 403 拒绝；设置、我的账号和按钮能力一致；授权开关须确认，放弃不写入；启用及采购更新均不改写已有成本历史 |
| 并发与故障 | 全量实库回归覆盖同版本并发保存、采购并发启用、撤权后等待中的启用、事务回滚、一致读快照、数据库运行锁丢失与在途写入拒绝 |
| 完整恢复 | 业务变更后在线生成原生快照，恢复至另一空库／空附件目录；58 表内容、顺序、序列和原件引用校验通过，原账号登录成功，23 期采购价格及手动成本一致 |

此次同机备份约 1.14 秒、恢复约 2.16 秒，仅作为本机样本耗时，不代表服务器迁移停机时长。原生恢复使用完整快照与显式附件路径映射；未把 SQLite 运行文件混入 PostgreSQL。写入闭环和恢复演练属于测试证据，不保留在当前交付页面。

### 检查与交接

完整后端 **476 项通过，无失败、错误或跳过**；前端 **37 项通过**，类型检查、隔离构建、安全扫描、测试数量门禁、编译、文档链接和 Git 差异检查通过。保留原有 Starlette/httpx 弃用提示。浏览器首次登录前的 `/api/me` 401 属预期；未发现运行错误。此次未修改应用源码、数据库结构或前端样式。

浏览器验收覆盖采购未保存退出确认、价格保存／启用、两个研发编辑入口、原因与比例、手动成本、草稿锁定／取消／重开、最终核对、设置授权提示及我的账号；截图在 `output/playwright/pg-step5-*.png`。自动化辅助脚本曾因页面标签、异步等待和路由释放断言失败，已按实际页面修正并复验，未作为产品回归或忽略失败。底层业务边界与并发覆盖以完整接口用例为准，本轮未重新展开组件改造。

测试报告、闭环、重启对照、恢复快照及旧测试库最终备份，已归档至项目同级 `2026-08-17_企业AI中台独立研发-postgresql-migration-archive/step5-tests-20260915T003405Z/`。清单记录原始路径、SHA-256 和恢复说明，132 个文件逐项校验并抽样恢复通过。旧 UI 和恢复演练数据库已删除；本地旧文件删除被自动审批拒绝（`blocked by policy`），仍保留在忽略目录，不提交、不迁移绕过拦截。账号和权限未新增或重置，专用测试库为空业务结构。

交付前重新对照当前正式 SQLite：58 表完整迁入，56 张业务／账号／历史表内容及顺序一致；会话和登录审计两表只有对照登录产生的差异。98 项读取接口及 65 个产品同输入试算完全一致；采购和研发三种时间范围、双成本差异三个方向、待处理状态均一致。实际浏览器对照采购价格历史、研发看板、产品成本台账及成本历史，显示内容一致，无页面运行错误。

当前采购共 21 版，最新 V21；研发 65 个产品，历史共 22 条（1 期正式核算及 21 期历史回算），无待启用草稿。旧测试 V22／V23、手动成本和测试草稿不在当前交付库。核验记录位于 `.scratch/postgresql-step5/clean-review/` 的 `api-alignment.json`、`simulation-comparison.json`、`rank-alignment.json`、`ui-alignment.json`、`final-boundary.json`、`cleanup.json`。只读核验确认正式配置、dist、原项目 Git 及各原服务保持正常。8061 保持登录，业务数据与正式一致；用户补充要求的授权记录末尾去线仅在隔离前端生效，类型检查及构建通过。

第五步当时完成隔离验收，未切换正式服务。后续已按下方第六步执行：冻结正式写入，重新备份当前正式库，再迁入正式 PostgreSQL 空库，未复制含验证活动的验收库。

### 2026-09-15 快速复验

再次实时比较 8000 与 8061：97 项读取接口、65 个产品同输入试算及各范围排行一致。另从当前验收库生成临时副本，采购两次录价启用、研发自动同步、原因校验、投料／收率编辑、草稿保存／重开／取消／再保存、普通编辑拒绝启用、管理人员启用、零金额和高精度手动值传递、采购更新保留手动值、旧确认失效、恢复自动和历史冻结全部通过；66 条原核算记录未改写。闭环后临时数据库已移除。

最终对照确认 56 张业务／账号／历史表仍与正式源一致；会话及登录审计两表仅有预期登录差异。采购仍为 V21，研发仍为 65 个产品、22 条版本历史、无草稿，各服务正常。未发现新增问题，未修改应用代码或正式运行状态。复验凭据保留在 Git 忽略目录 `.scratch/postgresql-step5/quick-recheck/`，入口为 `summary.json`。

## 第六步：正式切换与收尾

2026-09-15，用户授权完善日志、替换正式服务并收尾。先验收 9 月 15 日日志和授权记录末尾去线，再停止旧服务，从最新 SQLite 生成冻结备份，迁入已明确标记为 `production` 的空 PostgreSQL 业务库。

58 表、13,244 条记录全部迁入且内容、主键、顺序一致，原账号及权限不重置；相对初始基线的增量只有会话和登录审计。旧 SQLite、配置及原程序文件继续保留。切换前后 97 项接口和 65 个产品试算相同，4 个业务页面与干净预览一致，正式原因拦截、取消、模块锁定、更新日志和授权记录显示通过。正式快照覆盖原生转储、运行配置与采购原件，文件及环境校验通过。

正式入口仍为 8000，程序从独立版本目录运行，运行依赖不引用工作树或 `.scratch`。第六步证据在 `.scratch/postgresql-step6/`；部署位置、恢复边界、检查结果与清场执行结果统一见 [正式发布记录](正式发布-2026-09-15.md)。

用户随后确认清场及提交推送：8050、8060、8061 的旧实例已停止，两个验收库在原生备份及逐表验证后移除。913 个证据文件已归档并逐项校验、抽样恢复通过；文件删除仍被自动审批拒绝，原件保留在 Git 忽略目录。正式服务及 56 张业务、账号、权限和历史表复核正常。源码经 `codex/postgresql-migration` 分支交付，本轮不合并 main；准确提交与同步状态以 Git 和部署标记为准。
