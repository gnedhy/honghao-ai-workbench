# 当前交接

2026-09-15，经用户确认已切换正式 PostgreSQL 服务，8000 已完成实际核验。部署、启动及恢复位置统一以 [9 月 15 日发布记录](正式发布-2026-09-15.md) 为准；六步过程见 [迁移记录](PostgreSQL迁移.md)。不要再按旧交接启动 SQLite 正式服务。

## 现役与源码

- 正式入口为 `http://127.0.0.1:8000/`，数据库身份为 `production`，库名 `honghao_production`，服务仅使用应用账号。
- 正式程序位于 `C:/ProgramData/HonghaoAI/application/releases/20260915-postgresql`，附件与配置位于同级 `application/production-data`；独立依赖与前端已固化，不依赖 `.scratch` 或迁移工作目录运行。
- PostgreSQL 迁移提交 `7cc1d6b` 已按用户要求合入 `main`，后续从主线检出；准确同步状态以 Git 为准。正式程序仍从独立版本目录运行，合并源码不代表重新部署或启动服务。
- 原项目目录已更新为 PostgreSQL 源码。SQLite 回退源码保留在 Git 基线 `961f2020` 和本地 `.scratch/merge-main-20260915/sqlite-source-961f202.zip`，两份原本地说明已备份并存入 Git stash；旧程序、前端、SQLite 和冻结快照仍按发布记录保留。新正式库发生业务写入后，不能直接恢复过期 SQLite。

## 已核验

停写后的最新 SQLite 共 58 表、13,244 条记录，迁入后逐表内容、主键及顺序一致；14 个原账号、权限和采购原件全部保留。采购 V21、194 项原料、研发 65 个产品、22 条历史（1 期正式核算及 21 期回算），无测试草稿或测试版本。

切换前后 97 项接口、65 个产品同输入试算一致；采购价格历史、研发看板、成本台账与历史的页面内容和预览一致。编辑原因、取消、模块锁定、更新日志及授权末尾分隔线已实际检查。正式原生备份已完整校验；此前隔离闭环、恢复与 476 项后端检查通过，本次前端 37 项、类型、构建及安全检查通过。

业务规则与公共组件不因存储迁移改变。继续查 [组件索引](components.md)、[研发成本与授权](研发受控成本调整-2026-09-14.md)、AGENTS、CONTEXT 和 ADR-0010；部门和负责人不授予业务权限。

## 隔离现场与收尾

用户确认清场后，已核对并停止 8050 旧预览、8060 SQLite 恢复实例及 8061 PostgreSQL 验收实例。两个验收库 `honghao_acceptance`、`honghao_acceptance_step5_review_1767e4356a` 已完成原生备份、逐表验证后移除；正式库、开发库与专用测试库保留。

本轮 913 个本地证据文件已在项目同级 `2026-08-17_企业AI中台独立研发-postgresql-migration-archive/cleanup-20260915/` 归档，逐项校验并抽样恢复通过；内含两个验收库的原生快照及恢复说明。第五步的 132 个文件归档亦保留。文件删除被自动审批拒绝（`blocked by policy`），原件继续在 Git 忽略目录保留，不通过迁移绕过拦截，未释放这部分磁盘空间。原项目 `.scratch/repository-only-check/node_modules` 是目录联接，整理时不得递归跟随。

清场后正式服务仍就绪，56 张业务、账号、权限及历史表保持一致。保护正式库、凭据、运行目录、旧 SQLite 和恢复备份；数据、密码、日志、截图及归档不进入代码提交。执行回执为本地 `.scratch/postgresql-closeout/cleanup-result.json`，外部归档保留副本；长期记忆未修改。

## 工作台加载动效更新

2026-09-15 11:17 经用户确认完成正式前端替换，当前入口 `index-DRAkEaw7.js`。采购与研发加载动效及业务名称统一，正式页面核验通过；恢复备份与验证记录见 [当日发布记录](正式发布-2026-09-15.md#1117-工作台加载动效正式替换)。源码随本次加载动效提交保存。
