# 当前交接

## 现役入口

先读根目录 AGENTS、README 与 CONTEXT。功能口径见 ADR-0008、ADR-0009 和 [研发受控成本调整](研发受控成本调整-2026-09-14.md)；发布事实见 [正式发布记录](正式发布-2026-09-14.md)。PRODUCT 面向产品范围，DESIGN 面向共用视觉约定，均保留独立职责。

正式服务使用 `C:/Users/gnedhy/AppData/Local/HonghaoAI/production`，由仓库 Python 服务提供仓库 `dist`，入口 `http://127.0.0.1:8000/`。环境由配置、数据目录及 live readiness 核实，不能只按端口判断。构建先指定隔离输出目录，避免覆盖运行中的前端。

## 当前规则

- 研发五部支持原因记录、两口径自动或手动成本、草稿保存锁定与试算恢复、引用链传播和历史冻结。
- 林菲菲为研发管理及启用授权管理人员；四位研发编辑初始无独立启用权。实时授权以账号接口为准。总经办新增三位系统管理员，不在文档保存密码。
- 采购和研发启用授权均先确认再生效；独立授权不授予转授权能力或配方停用权限。
- 未启用的功能模块对所有管理员锁定并从设置隐藏；总经办工作台开关统一锁定。已启用模块和其它工作台保持原规则。
- 排行、关于系统等界面按 9 月 14 日最终验收结果维护。历史日志不作为当前权限说明。

## 收尾状态

用户已授权整理 README 后提交并推送，提交与同步状态以 Git 记录为准。未修改机器生成记忆。历史资料外部归档及待清场清单见 [收尾报告](收尾报告-2026-09-14.md)。正式数据、运行依赖及源码保留；删除和停止预览须完成具体清单复核后确认。

正式前端已于 2026-09-14 16:50 更新日志并完成全量复验。注意正式服务仍引用 `.scratch/release-module-lock-20260914-162553/service.out.log` 与 `service.err.log`，不能整目录删除 `.scratch`；具体例外见收尾报告。

最新前端已补发研发使用说明（index-B6IzdmOs.js）；沿用此前全量业务复验，本次纯说明补充另通过类型、构建和浏览器检查。新增构建与恢复备份已并入外部归档 manifest-使用说明.json，仍待清场确认。

清场已获用户确认，但停预览／删除命令被自动审批以 blocked by policy 拒绝；未删除或迁移，8050 仍运行。不要重复请求同一清场授权，也不要通过迁移绕过拒绝；见收尾报告执行状态。

仓库范围已整理：原开工／设计目录取消跟踪并忽略，本地原件保留；历史 Markdown 放在 docs/history。提交与推送按用户最新授权执行。仓库文件副本构建通过；`.scratch/repository-only-check/node_modules` 是指向现役依赖的验证用目录联接，移除请求被平台策略拒绝，后续整理禁止递归跟随该联接。
