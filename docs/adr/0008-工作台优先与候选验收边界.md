# 工作台优先与候选验收边界

产品范围与隔离原则继续有效；下述 9 月 9 日端口、数据基线、账号例外及提交授权均为当次记录。存储与迁移约定见 [ADR-0010](0010-PostgreSQL原生存储与隔离迁移.md)，当前状态见 [当前交接](../当前交接.md)。

2026-09-09，用户确认本轮采购工作台验收，并要求先完成文档与远程、候选库对比，再提交和推送。

当前产品以职能工作台为核心，优先完成采购业务闭环；知识库、AI 会话、任务、自动化及智能体能力以后按实际需要决定加入、调整或删减。早期路线图保留为历史，不作为继续开发这些模块的默认授权。本轮不删除其他模块的已有实现或数据。

整体程序和采购数据以 8012 已验收版本为准。候选库保留旧库的非采购项目、会话、消息和任务，不把旧采购实现或价格混入新基线。8020 是候选数据的隔离验收副本，其试操作不自动成为正式数据；实际环境由目录标记与服务配置判断，而非端口号。

用户明确要求保留 gnedhy 的原密码：候选库恢复该账号原密码校验值，既有原库不变，无需收集明文。该要求是本次候选验收的账号例外，不扩展为复制其他测试账号密码、会话或密钥的授权。

本次授权提交并推送当前开发分支，不包含合并主分支、正式启用或删除复核现场。正式启用继续遵守 ADR-0007 与模块启用审查清单；缺失的审查证据必须真实补齐，不得伪造记录。
