# 用户反馈

2026-09-10 后续批准：新建表单采用 `.impeccable/mocks/feedback/20260910-form/index.html` 的 A 单列方案；类型分段单选，正文弹性占位、截图统一上传区域、隐私说明固定在操作栏。管理员本人筛选采用 aria-pressed 按钮，状态菜单复用采购 columnMenu/personMenu，保持筛选语义。窄屏单列且操作栏换行。动效按用户批准复用系统设置 dialog-in、fade-in、reduced-fade-in 与 ease-out；关闭 150ms 后卸载并恢复焦点，减少动态效果时立即关闭且禁用反馈区域动画。正式构建 `index-BFxMPCI2.js`，打开、新建、关闭及焦点恢复已在正式页面检查；无正式反馈测试写入。

采用已批准的搜索右侧常驻消息入口及固定头尾反馈窗口；管理员单列表、本人筛选，普通用户仅本人内容。正式实现复用 SettingsDialog 的 DiscardChangesDialog，替换预览的页内放弃条；不改全局设计。

未读记录持久化且与处理状态分离。关闭、返回、Esc、刷新保护草稿；保存失败保留输入，409冲突重新取当前版本并保留编辑内容。截图单张5MB，服务端实际解码校验。管理员完成必须填写公开处理结果。

桌面固定740×650，窗口按矮屏收缩，头尾始终可见，长正文仅内容区滚动。18项反馈及个人资料相关测试、真实候选账号闭环、桌面与390px页面检查通过。独立安全审查未发现阻断，正式用户内容不进入测试。

用户本轮已授权正式发布；发布事实和回退路径见 docs/用户反馈正式部署-2026-09-10.md。
