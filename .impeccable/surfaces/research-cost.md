# Research cost workbench

- Route: evolve / operate / large. User approved A 台账＋抽屉 and the second 19-annotation implementation plan on 2026-09-12. Scope includes dashboard, dual ledger, 21 historical replay periods, immutable formal history, nested detail, formula lifecycle management, persistent drafts and managed activation.
- Audience: RD5 viewers (level2), editors (level3), managers (level4), system administrators. Source owners are attribution, not permissions.
- Preserve the live procurement shell, typography, table row height, menus, pagination and restrained color system. Global segmented controls now use the approved feedback-style white selected fill, pale-gray track, subtle shadow and existing motion/reduced-motion behavior; DESIGN.md and design.json updated together. No dependency added.
- Keep existing 65 complete codes separately. Navigation is 研发看板 / 产品成本 / 配方管理 / 价格历史. New unique full codes enter as drafts; inactive records retain IDs and history. Three source trials stay independent from formal records and replay.
- Columns: 产品内编 / 负责人 / 产品成本（最新优先） / 产品成本（库存优先） / 价格变化 / 收率 / 投料明细 / 状态. Code opens the drawer; no redundant view column. No name/owner/status sorting. Default change descending, null last, code breaks ties; old sort preferences migrate without losing column/page settings. Money displays and hovers two decimals; comparisons round both operands to cents.
- Dashboard combines the frozen V1–V21 replay and actual formal records, marked 含历史回算. Trend defaults to three months, interval movers fourteen days with explicit missing endpoint behavior. Never generate example trends. Compact preview fills the unused task-card width; full view retains tasks. Time rankings retain two five-item columns; the approved current dual-cost gap card sits to their right on desktop. Both cards stack on narrow screens.
- Draft work stays out of formal ledger and history. Simulate before save; explicit final review before manager activation; stale input conflict retains the user's form. Referenced products and approved composite drill into their own cost under the same policy.
- Narrow view: wrapping toolbar and contained horizontal table scrolling; drawers fit the viewport, actions remain reachable. Test desktop and mobile after final implementation.
- History uses the purchase-style timeline and version detail; product rows expand frozen dual costs and inputs inline. Current-price fallback is labeled as such, never described as historical inventory. Ledger/current statistics/version details compare against the last distinct rounded price; unchanged prices retain that movement. Interval rankings compare their selected endpoints. Old formal and replay payloads remain immutable; input deduplication only controls creation of new formal records.
- Mobile menus stay inside the viewport; dates remain single-line, custom date inputs and apply action remain reachable. Editor footer margins match mobile drawer padding; only the feed table scrolls horizontally.
- Authority: user-approved plan and current procurement CSS. Second revision independently reviewed and deployed on 2026-09-12 12:34 after backup and restore drill; no pending design direction or findings. Evidence: `.impeccable/review/research-cost/current.json`, including desktop, 390×844 and production verification captures. See `docs/研发工作台调整-2026-09-12.md`.

## 2026-09-12 trend calibration

- Mode calibrate / operate / small. User comment explicitly approves preserving dual lines and matching procurement curves. Reuse procurement monotone interpolation, 2px strokes, white-filled 3px nodes, 4px active nodes and quiet axes; retain existing dark-gray/latest and blue-gray/inventory identities. Add matching line/node legend with both labels and 元/kg. Preserve costs, gaps, dates, interval ranking, tooltips and both full/preview entry points.
- At 390px the two legend items fit the chart card; wrapping remains available for increased text size. No global token or component change.
- The user subsequently approved the bottom-right double-cost gap panel; the approval and implemented scope are recorded below.

## 2026-09-12 dual-cost gap panel

- Evolve / operate / medium, approved by the user replying 可以 to the concrete two-cost difference proposal. Reuse existing card, white segmented controls, typography and product drawer. Bottom row keeps the original interval top-ten list (two groups of five) on the left; adds current gap top-five on the right. At viewport widths <=1200px or dashboard content widths <=1000px the cards stack; within <=600px content the temporal list also becomes one column. This covers the narrower home preview. Mobile retains all controls and labels. Compact preview also includes both cards, with full-width trend retained.
- Latest-first minus inventory-first cost, in yuan/kg. Default all differences sorted by absolute gap between rounded two-decimal costs, ties by full product code; optional latest-higher and inventory-higher filters. Show both current costs, signed gap and two-decimal amounts on hover. Equal costs excluded; explicit zero valid; missing, nonfinite and nonready costs excluded. Do not describe amount gap as temporal movement or savings; no date control on the current-cost card. Clicking a product opens the existing drawer. Existing temporal comparison chain remains unchanged.
- Types/build and two focused analytics tests pass; independent desktop/mobile review and live release evidence are under the current manifest. No backend/data/config changes or new dependencies.

## Current comparison authority

See `docs/研发工作台调整-2026-09-12.md`, section 最新确认. Final controls read 全部 / 最新优先 / 库存优先. The trend, interval ranking, current gap and simulation have distinct purposes; all monetary comparisons use rounded cents, while internal formula calculation and frozen inputs retain precision. Latest implementation captures remain in `20260912-rounded-comparison`; documentation-only reconciliation and application changelog evidence are recorded separately in `.scratch/closeout-20260912/`. Historical manifests are not rewritten to claim a new review.
