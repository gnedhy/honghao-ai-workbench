# 宏昊 AI 工作台

宏昊 AI 当前以职能工作台为核心，优先交付采购等确定性业务闭环。知识库、AI 会话、任务与自动化均为后续待评估能力；以下概念解释已存在的边界，不代表本轮实施顺序或全部已启用。当前范围见 ADR-0008。

## 交互与执行

**会话（Conversation）**：
用户与个人智能体持续交流的容器，可以选择关联一个轻项目；一条会话可以发起多个任务。
_避免使用_：聊天任务、工作项目

**聊天**：
面向即时问答和内容协作的交互意图，不创建持续执行任务。
_避免使用_：普通模式、闲聊模式

**工作**：
面向计划、工具调用、暂停恢复或人工确认的交互意图；每次提交都会创建一个任务。
_避免使用_：任务模式、项目模式

**任务（Task）**：
由一次工作提交创建的持久执行对象，始终记录来源会话，并在创建时确定自己的项目关联。
_避免使用_：工作、对话、运行

**待执行（created）**：
任务已经持久化，但尚未开始首次任务运行的初始状态。
_避免使用_：已创建、等待运行

**工作提交键（SubmissionKey）**：
一次聊天或工作提交的稳定幂等标识；相同提交键的重试只能返回原结果，不能重复创建消息或任务。
_避免使用_：请求 ID、提交 ID

**任务运行（TaskRun）**：
任务的一次具体执行尝试；同一任务可以因恢复或重试产生多次运行。
_避免使用_：任务、流程

**智能体执行器（Harness）**：
承载一次任务运行所需上下文、能力边界、执行证据和人工关口的受控容器；模型、技能和工具都必须通过它参与执行。
_避免使用_：智能体、工作流、模型接口

**执行循环（Loop）**：
智能体执行器在一个工作流节点内进行的有界迭代，每轮观察结果后决定继续、等待或停止，并始终给出明确停止原因。
_避免使用_：工作流、无限循环、自动重试

**停止原因**：
一次执行循环结束或中断的业务原因，用于区分正常完成、等待用户、等待确认、策略阻断、预算耗尽、失败和取消。
_避免使用_：任务状态、错误码

**等待原因**：
任务等待用户介入的明确原因，分为等待补充信息、等待诊断审阅和等待修改确认。
_避免使用_：统一等待审批、暂停

## 轻项目

**轻项目（Project）**：
聚合相关会话、任务、知识来源和受控目录的可选上下文，不承担成员、排期、里程碑等项目管理职责。
_避免使用_：项目管理、工作区、文件夹

**当前项目上下文**：
用户当前浏览和新建工作的默认项目，只影响筛选与后续创建，不自动改写已有会话、任务或知识的项目关联。
_避免使用_：项目归属、全局项目

**项目关联**：
会话、任务或知识与轻项目之间各自保存的可选关系；实体已有的历史关联不会因界面切换而自动迁移。
_避免使用_：当前项目、项目所有权

**项目卡**：
轻项目的结构化业务档案；诊断对应已有项目时更新该档案，未关联项目时经确认后创建轻项目。
_避免使用_：诊断卡、知识条目

## 知识与能力

**知识库（KnowledgeSpace）**：
承载个人知识或公共知识的明确边界。
_避免使用_：项目空间

**知识来源（KnowledgeSource）**：
上传后不可覆盖的原始文件及其来源元数据；现阶段仅接收 PDF，必须先处于隔离状态并完成安全确认后才能解析。
_避免使用_：知识正文、附件副本

**知识版本（KnowledgeVersion）**：
由一个或多个知识来源派生、等待审阅的 Markdown 版本；保留来源 ID、文件名和哈希，不能替代或覆盖原文件。
_避免使用_：原文件、已发布知识

**知识条目（KnowledgeItem）**：
已经进入知识库、具有来源和版本的一项正式知识资产。
_避免使用_：知识候选、任务结果

**知识候选**：
任务提出但尚未写入知识库的待确认内容，个人知识候选与公共知识候选分别处理。
_避免使用_：草稿知识、已沉淀知识

**技能（Skill）**：
完成一项可复用能力的版本化定义，由任务调用，不归属于某个轻项目。
_避免使用_：工具、智能体、插件

**工作流（Workflow）**：
连接多个业务阶段和人工关口的固定外层流程，由任务执行，并在需要智能判断或工具调用的节点使用智能体执行器。
_避免使用_：技能、任务、自动化脚本

## 身份与访问

**运行环境（RuntimeEnvironment）**：
一套独立运行的系统实例，只分为测试环境与正式环境；两者不得共享账号、数据目录、数据库、文件或密钥。
_避免使用_：模块原型、发布状态、功能开关

**待生效模块状态**：
管理员已经保存、但要等当前服务重启后才会应用的模块状态；不改变正在运行的入口和接口。
_避免使用_：立即启用、开发环境、已发布

**本地账号（UserAccount）**：
企业内部人员登录系统使用的实名账号，由系统管理员创建和停用，不允许自助注册。
_避免使用_：模型账号、外部账号、访客

**业务权限（AccessLevel）**：
账号在一个授权范围内的操作深度，只分为查看、编辑和管理：查看只能读取授权数据；编辑可新建、修改、导入和提交，但不能使结果正式生效；管理可审核、退回、确认、发布及管理范围内业务。系统管理独立于业务权限。
_避免使用_：L1-L5、用户角色、岗位等级、全局业务等级

**采购本轮更新**：
所有录价人员共同维护的一轮待启用价格；保存不改变正式价格，每次保存的参与者和前后值均可追溯。
_避免使用_：个人草稿、原料负责范围

**价格启用权**：
确认采购异常波动、立即或定时启用价格及撤销排期的独立权限，不包含账号、目录或整轮取消权限。
_避免使用_：采购编辑权、采购管理员

**授权管理人员**：
可授予或撤销采购价格启用权、并可取消整轮更新的人员；普通获授权者不能转授权。
_避免使用_：全部启用人、采购负责人身份

**数据分流**：
各部门按原料清单引用同一份当前正式价格的管理入口；部门关联不产生价格副本。
_避免使用_：部门台账副本、手工同步任务

**正式价格版本**：
已启用或历史迁入的不可变价格快照，只有最新版本是当前正式基线；价格变化比较前一正式版本。
_避免使用_：报价汇总、修改次数

**原表采购员**：
来源文件记载的采购员姓名，不等同于系统录价或启用操作人，也不决定访问权限。
_避免使用_：系统操作人、历史启用人

**授权范围（AccessScope）**：
账号可以进入的业务区域，可同时包含采购、研发、销售、总经办工作台和知识库；每个范围独立保存自己的业务权限。
_避免使用_：部门权限、岗位权限、菜单开关

**权限策略（AccessPolicy）**：
服务端按“模块状态 → 授权范围及其等级 → 资料访问规则 → 敏感字段策略”依次执行的访问规则；前端隐藏入口不能替代服务端校验。
_避免使用_：前端权限、角色权限、部门权限

**敏感字段（SensitiveField）**：
由业务模块预置或系统管理员登记、可分别配置查看/编辑最低权限及开放范围的受控业务字段；编辑最低权限只能是编辑或管理，未配置开放范围时仅系统管理员可访问。自定义登记只创建权限标识，业务模块仍必须显式映射对应字段。
_避免使用_：任意数据库列、页面字段、脱敏标签

**审计事件（AuditEvent）**：
安全相关操作留下的主体、对象、动作和时间记录，不保存密码、正文或敏感字段值。
_避免使用_：业务日志、操作正文、完整请求

**登录会话（LoginSession）**：
账号成功登录后由服务端创建的限时访问凭证，与承载智能体交流的会话（Conversation）无关。
_避免使用_：会话、对话、浏览器账号

## 确认与写入

**待确认修改（ChangeSet）**：
正式写入前供用户审阅的一组目标明确、可批准或拒绝的修改；项目卡、个人知识候选和公共知识候选分别生成。
_避免使用_：变更集、直接写入、审批单

**已完成但未应用**：
任务流程正常结束，但用户拒绝了待确认修改，因此没有产生对应的正式写入。
_避免使用_：失败、已应用、已取消

**受控工作目录**：
智能体被明确授权读写的文件边界；任务可以在项目目录内进一步收窄范围，但不能扩大权限。
_避免使用_：任意目录、项目文件夹
